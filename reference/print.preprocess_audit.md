# Print method for preprocessing audits

Prints a concise summary of variables, ordered rows, retained lag pairs,
trend flags, high-AR flags, and zero-variance flags.

## Usage

``` r
# S3 method for class 'preprocess_audit'
print(x, ...)
```

## Arguments

- x:

  A `preprocess_audit` object.

- ...:

  Ignored.

## Value

`x`, invisibly.
