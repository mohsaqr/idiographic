% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usem.R, R/var.R, R/vendored.R
\name{edges.net_usem}
\alias{edges.net_usem}
\alias{edges.var_result}
\alias{edges}
\alias{edges.netobject}
\alias{edges.netobject_group}
\alias{edges.gvar_result}
\alias{edges.net_mlvar}
\alias{edges.net_gimme}
\title{Tidy edge table for any idiographic result}
\usage{
\method{edges}{net_usem}(x, sort_by = "weight", include_self = FALSE, ...)

\method{edges}{var_result}(x, sort_by = "weight", include_self = FALSE, ...)

edges(x, ...)

\method{edges}{netobject}(x, sort_by = "weight", include_self = FALSE, ...)

\method{edges}{netobject_group}(x, sort_by = "weight", include_self = FALSE, ...)

\method{edges}{gvar_result}(x, sort_by = "weight", include_self = FALSE, ...)

\method{edges}{net_mlvar}(x, sort_by = "weight", include_self = FALSE, ...)

\method{edges}{net_gimme}(
  x,
  sort_by = "weight",
  include_self = TRUE,
  weight = c("prop", "coef"),
  ...
)
}
\arguments{
\item{x}{A \code{gvar_result}, \code{net_mlvar}, \code{net_gimme}, \code{netobject}, or
\code{netobject_group}.}

\item{sort_by}{\code{"weight"} (descending |weight|) or \code{NULL} for natural order.}

\item{include_self}{Keep autoregressive self-loops? Default \code{FALSE}
(\code{TRUE} for GIMME, where the autoregression is the point).}

\item{...}{Passed to methods.}

\item{weight}{For GIMME only: \code{"prop"} (proportion of subjects, default) or
\code{"coef"} (group-average coefficient) for the edge weight.}
}
\value{
A tidy \code{data.frame}, one row per edge.
}
\description{
A single tidy verb for every network idiographic produces. Returns one row per
edge with columns \code{network} (e.g. \code{"temporal"}, \code{"contemporaneous"},
\code{"between"}), \code{from}, \code{to}, \code{weight} -- and, for GIMME, \code{level}
(\code{"group"}/\code{"individual"}). Directed networks (temporal) keep every edge;
undirected networks (contemporaneous, between) report each pair once.
}
\examples{
\dontshow{if (requireNamespace("graphicalVAR", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(1)
d <- data.frame(id = 1, A = rnorm(80), B = rnorm(80), C = rnorm(80))
fit <- graphical_var(d, vars = c("A", "B", "C"), id = "id", n_lambda = 8)
edges(fit)            # tidy: network / from / to / weight
}
\dontshow{\}) # examplesIf}
}
