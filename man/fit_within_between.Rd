% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_within_between.R
\name{fit_within_between}
\alias{fit_within_between}
\title{Fit a within-between (hybrid) model}
\usage{
fit_within_between(
  data,
  y,
  x,
  id,
  time = NULL,
  model = c("within_between", "within", "between", "contextual"),
  estimator = c("ols", "reml", "ml"),
  scope = "pooled",
  subgroup = NULL,
  random = NULL,
  cluster = NULL,
  conf_level = 0.95,
  test_prop = 0.2,
  min_train = 10L,
  min_test = 1L
)
}
\arguments{
\item{data}{Data frame.}

\item{y}{Outcome column name.}

\item{x}{Predictors: names, numeric positions, \code{a:b} range, formula, or data frame.}

\item{id}{Person/unit ID column.}

\item{time}{Optional ordering column.}

\item{model}{Parameterization: \code{"within_between"}, \code{"within"}, \code{"between"},
or \code{"contextual"}. See Details.}

\item{estimator}{\code{"ols"} (cluster-robust least squares), or \code{"reml"} / \code{"ml"}
for a mixed model via \code{lme4}.}

\item{scope}{\code{"pooled"}, \code{"subgroup"}, or \code{"all"}. A between-person term is
constant inside a person, so individual scope is available only for
\code{model = "within"}.}

\item{subgroup}{Optional subgroup mapping: an \code{\link[=find_subgroups]{find_subgroups()}} result, a
grouping column in \code{data}, or a named vector of labels per person.}

\item{random}{Extra grouping columns to give random intercepts, e.g.
\code{random = "course"} for a cross-classified design. Mixed estimators only.}

\item{cluster}{Column(s) to cluster standard errors on, defaulting to \code{id}.
Up to two, for two-way clustering. OLS only.}

\item{conf_level}{Confidence level for the reported intervals.}

\item{test_prop}{Proportion of each person's ordered rows held out.}

\item{min_train}{Minimum complete training rows per person.}

\item{min_test}{Minimum complete held-out rows per person.}
}
\value{
An \code{idiographic_wb} object, which is also an \code{idiographic_fit}. \code{coefs()}
gains \code{component} and \code{variable} columns; \code{\link[=contextual]{contextual()}} returns the
within/between comparison.
}
\description{
Puts the within-person and the between-person component of a predictor into
\strong{one} model, so the two effects can be compared directly and the gap
between them tested. Person-centering alone cannot do this: it estimates the
within effect and discards the between effect entirely.
}
\details{
Each predictor \code{x} is split into a person mean (the between component) and
the deviation from it (the within component).
}
\section{Model parameterizations}{

\describe{
\item{\code{within_between}}{Both components as separate terms (the default).
The contextual effect is their difference, tested as a contrast.}
\item{\code{within}}{The within component only -- the fixed-effects estimator.
The only parameterization that also works at individual scope.}
\item{\code{between}}{The between component only.}
\item{\code{contextual}}{The raw predictor plus the person mean. Algebraically
equivalent to \code{within_between}, but the coefficient on the person mean
\emph{is} the contextual effect directly, rather than a contrast of two.}
}
}

\section{Estimators}{

\code{"ols"} (the default, base R) fits by least squares with \strong{cluster-robust}
standard errors; rows within a person are not independent, and treating them
as independent gives roughly 77\% coverage where 95\% is claimed. Pass
\code{cluster} to cluster on something other than -- or in addition to -- the
person; with two clustering variables the Cameron-Gelbach-Miller two-way
estimator is used, which is what a design with people crossed by courses
needs.

\code{"reml"} and \code{"ml"} fit a mixed model with \code{lme4} instead, adding a random
intercept for \code{id} and for anything named in \code{random}. This is what makes a
cross-classified design -- \code{(1 | person) + (1 | course)} -- expressible, and
it makes the model's variance components available through
\code{\link[=variance_components]{variance_components()}}. The fixed-effect estimates agree closely with the
OLS ones; what differs is the standard errors and the variance decomposition.

Person means are computed on the \strong{training} rows and applied to the
held-out rows, so the reported metrics stay honest.
}

\examples{
fit <- fit_within_between(srl, y = "effort", x = c("efficacy", "planning"),
                          id = "name", time = "day")
coefs(fit)
contextual(fit)

# The contextual parameterization reports the same gap as one coefficient.
fit_within_between(srl, y = "effort", x = "efficacy", id = "name",
                   model = "contextual")
}
