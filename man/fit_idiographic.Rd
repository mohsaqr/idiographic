% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{fit_idiographic}
\alias{fit_idiographic}
\title{Fit an idiographic model through the unified interface}
\usage{
fit_idiographic(data, method, ..., params = list())
}
\arguments{
\item{data}{A data frame or matrix passed to the selected method.}

\item{method}{A registered method name or alias.}

\item{...}{Named arguments passed directly to the selected method.}

\item{params}{A named list of additional method arguments.}
}
\value{
The selected method's result, unchanged except for lightweight
dispatch and equivalence metadata attributes.
}
\description{
\code{fit_idiographic()} dispatches every built-in estimator and workflow through
the same entry point. Arguments may be supplied directly or in \code{params},
which makes a stored configuration directly replayable. Direct arguments
and \code{params} must both be named and cannot overlap; this turns otherwise
ambiguous duplicate arguments into an immediate, informative error.
}
\examples{
set.seed(1)
d <- data.frame(A = rnorm(80), B = rnorm(80))
fit <- fit_idiographic(d, "var", vars = c("A", "B"), scale = FALSE)
fit2 <- fit_idiographic(d, "ols-var",
                        params = list(vars = c("A", "B"), scale = FALSE))
equivalence(fit)
}
