% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_subgroups.R
\name{find_subgroups}
\alias{find_subgroups}
\title{Discover subgroups of people}
\usage{
find_subgroups(
  data,
  y,
  x,
  id,
  method = c("effect_clustering", "error_clustering", "repeated_split", "model_tree",
    "mixture_regression", "random_partition"),
  k = 2L,
  reps = 50L,
  moderators = NULL,
  time = NULL,
  k_max = 6L,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{y}{Outcome column name.}

\item{x}{Predictors (any \code{\link[=fit_lm]{fit_lm()}} selector).}

\item{id}{Person/unit ID column.}

\item{method}{Discovery method.}

\item{k}{Number of subgroups, or \code{"auto"} to choose it by stability.}

\item{reps}{Resamples used for the stability score.}

\item{moderators}{Person-level column(s) to split on. Required by
\code{method = "model_tree"}, ignored otherwise. Must be constant within person.}

\item{time}{Optional ordering column.}

\item{k_max}{Largest \code{k} considered when \code{k = "auto"}.}

\item{...}{Ignored.}
}
\value{
An \code{idiographic_groups} object. Use \code{\link[=groups]{groups()}} for the tidy table.
}
\description{
Groups people who behave alike, so that a model can be fitted per subgroup
rather than pooling everyone or fitting everyone separately.
}
\details{
Methods:
\describe{
\item{\code{effect_clustering}}{Cluster people by their person-specific slopes.
People whose predictors act the same way land together.}
\item{\code{error_clustering}}{Cluster people by how a pooled model fails them
(error size and direction). Finds who the pooled model does not serve.}
\item{\code{repeated_split}}{Like \code{effect_clustering}, but slopes are recomputed
on repeated resamples, so unstable people are exposed.}
\item{\code{model_tree}}{Split people on person-level \code{moderators} (age,
condition, baseline score) to reduce prediction error. The only method
that says \emph{why} people differ, in terms of variables you measured.}
\item{\code{mixture_regression}}{A finite mixture of regressions fitted by EM,
with the \strong{person} as the mixing unit: every row a person contributes
counts towards that person's likelihood under each component, so people
are assigned whole. Unlike \code{effect_clustering} it does not need a
separate regression per person, so it can place people with too few
occasions to support one. \verb{$mixture} holds the component regressions and
the posterior probabilities, and \code{groups()} gains a \code{posterior} column --
how sure the model is of each assignment, which is a stronger statement
than how often a partition reproduces. Matches \code{flexmix::flexmix(y ~ x | id)} to the reported precision on log-likelihood, BIC and assignment.}
\item{\code{random_partition}}{Random labels. A null baseline: compare its
stability against a real method to see whether structure exists.}
}

\strong{These methods always return subgroups, including when there are none.}
Call \code{\link[=test_subgroups]{test_subgroups()}} first, or use \code{k = "auto"}, which is allowed to answer
"one population".

Stability is a consensus score: each person is repeatedly re-clustered on
resampled rows, and stability is how often they land with the same companions.
It measures whether a partition is \strong{reproducible, not whether it is real}.
In simulation, data containing no subgroups at all scored 0.89-0.97 --
because slicing a single smooth cloud of coefficients in half is highly
repeatable. Do not read a high stability as evidence that the groups exist.

With \code{k = "auto"} the number of subgroups is chosen by \code{\link[=test_subgroups]{test_subgroups()}}, a
Gaussian mixture on the person coefficients selected by BIC over both the
number of components and the covariance structure. It may return \strong{one}, in
which case a warning is raised and the partition should not be trusted. See
\verb{$selection} for the BIC table and \verb{$shape} for the skewness diagnostic.

\code{method = "mixture_regression"} defers to the same test rather than to its
own BIC, and the reason is worth knowing. On 30 simulated panels containing
\strong{no} subgroups -- one population whose slopes merely varied continuously --
the mixture's BIC chose more than one component \strong{30 times out of 30}, while
\code{\link[=test_subgroups]{test_subgroups()}} answered "one population" 30 times out of 30 and still
recovered two real classes when they were present. A mixture of regressions
fits a continuum of slopes better with two components than with one, and its
BIC has no device to tell that apart from genuine classes. The mixture's own
BIC table is still reported in \verb{$mixture_bic}, for comparison with a
\code{flexmix}-style workflow -- it simply does not get to choose.
}
\examples{
g <- find_subgroups(srl, y = "effort", x = "efficacy:monitoring",
                    id = "name", k = 2, reps = 10)
groups(g)

# Let the data choose how many subgroups there are.
auto <- find_subgroups(srl, y = "effort", x = "efficacy:monitoring",
                       id = "name", k = "auto", k_max = 4, reps = 10)
auto$selection
}
