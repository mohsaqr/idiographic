% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_effects.R
\name{effects.idiostats_effects}
\alias{effects.idiostats_effects}
\title{Tidy treatment effects}
\usage{
\method{effects}{idiostats_effects}(
  object,
  effect = NULL,
  contrast = NULL,
  scope = NULL,
  subject = NULL,
  subgroup = NULL,
  sort_by = NULL,
  decreasing = FALSE,
  n = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{\link[=fit_effects]{fit_effects()}} result.}

\item{effect}{Optional filter on the effect label, e.g. \code{"ATE"}.}

\item{contrast}{Optional filter on the contrast, e.g. \code{"b vs a"}.}

\item{scope, subject, subgroup}{Optional filters.}

\item{sort_by}{Optional column to sort by.}

\item{decreasing}{Sort order when \code{sort_by} is supplied.}

\item{n}{Optional number of rows.}

\item{...}{Ignored.}
}
\value{
A data frame of effects with confidence intervals.
}
\description{
A method on the \code{stats::effects()} generic, so it does not mask anything.
}
\examples{
set.seed(1)
d <- data.frame(id = rep(1:6, each = 40), day = rep(1:40, 6),
                x1 = rnorm(240), x2 = rnorm(240))
d$drug <- rbinom(240, 1, 0.5)
d$mood <- 2 * d$drug * (d$x1 > 0) + 0.5 * d$x1 + rnorm(240, sd = 0.5)
fit <- fit_effects(d, "mood", "drug", c("x1", "x2"), "id", scope = "pooled")
effects(fit, effect = "ATE")
}
