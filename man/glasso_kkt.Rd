% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glasso.R
\name{glasso_kkt}
\alias{glasso_kkt}
\title{Certify a graphical-lasso solution from its optimality conditions}
\usage{
glasso_kkt(
  x,
  S = NULL,
  rho = NULL,
  penalize_diagonal = NULL,
  zero = NULL,
  active_tol = 1e-08
)
}
\arguments{
\item{x}{A \code{glasso_result} from \code{\link[=glasso_fit]{glasso_fit()}}, or a precision matrix.}

\item{S}{Covariance the model was fit to. Required only when \code{x} is a bare
matrix; taken from the fit otherwise.}

\item{rho}{Penalty (scalar or \verb{p x p} matrix). Required only when \code{x} is a
bare matrix.}

\item{penalize_diagonal}{Logical, whether the diagonal was penalised.
Required only when \code{x} is a bare matrix.}

\item{zero}{Two-column matrix of hard-constrained \verb{(row, col)} index pairs,
as passed to \code{\link[=glasso_fit]{glasso_fit()}}. Taken from the fit when \code{x} is a
\code{glasso_result}. Constrained entries are excluded from the check: at a
hard-constrained edge the inactive-edge inequality does not apply, because
the equality constraint carries its own multiplier that absorbs the
residual. Checking them anyway reports optimal fits as non-optimal.}

\item{active_tol}{Magnitude above which an off-diagonal entry counts as
active. Default \code{1e-8}.}
}
\value{
A single non-negative number: the maximum absolute stationarity
violation. Values near zero certify optimality.
}
\description{
Returns the largest violation of the graphical-lasso stationarity (KKT)
conditions. Because the objective is strictly convex, a value near zero
certifies the global optimum \emph{directly from the estimand}, with no appeal to
any reference solver. For \code{W = Theta^{-1}} the subgradient conditions are

\itemize{
\item diagonal, unpenalised: \eqn{W_{ii} = S_{ii}};
\item diagonal, penalised: \eqn{W_{ii} - S_{ii} = \rho_{ii}};
\item off-diagonal with \eqn{\Theta_{ij} \neq 0}:
\eqn{W_{ij} - S_{ij} = \rho_{ij}\,\mathrm{sign}(\Theta_{ij})};
\item off-diagonal with \eqn{\Theta_{ij} = 0}:
\eqn{|W_{ij} - S_{ij}| \le \rho_{ij}}.
}
}
\examples{
set.seed(1)
x <- matrix(rnorm(200 * 4), ncol = 4)
fit <- glasso_fit(cov(x), rho = 0.05)
glasso_kkt(fit)
}
\seealso{
\code{\link[=glasso_fit]{glasso_fit()}}
}
