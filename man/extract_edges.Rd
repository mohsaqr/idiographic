% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vendored.R
\name{extract_edges}
\alias{extract_edges}
\title{Tidy edge table from a network object}
\usage{
extract_edges(model, sort_by = "weight", include_self = FALSE)
}
\arguments{
\item{model}{A \code{netobject} or \code{cograph_network}. Multi-network results
(a \code{gvar_result}, \code{net_mlvar}, or any \code{netobject_group}) hold more than one
network, so pass a single constituent — e.g.
\code{extract_edges(as_netobject(x)$temporal)}.}

\item{sort_by}{Either \code{"weight"} (descending by absolute weight) or \code{NULL}.}

\item{include_self}{Keep autoregressive self-loops? Default \code{FALSE}.}
}
\value{
A \code{data.frame} with columns \code{from}, \code{to}, \code{weight}.
}
\description{
Returns a one-row-per-edge \code{data.frame} with node labels, for any
netobject / cograph_network (or a \code{gvar_result} constituent).
}
