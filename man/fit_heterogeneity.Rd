% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_heterogeneity.R
\name{fit_heterogeneity}
\alias{fit_heterogeneity}
\title{Study how something varies across people}
\usage{
fit_heterogeneity(
  data,
  y,
  x,
  id,
  target = c("cate", "error", "gain"),
  treatment = NULL,
  model = c("linear", "ridge", "tree"),
  estimator = "native",
  time = NULL,
  num_splits = 100L,
  prop_aux = 0.5,
  n_groups = 4L,
  clan = NULL,
  split = c("person", "occasion"),
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{y}{Outcome column name.}

\item{x}{Predictors: names, numeric positions, \code{a:b} range, formula, or data frame.}

\item{id}{Person/unit ID column.}

\item{target}{What varies across people: \code{"cate"}, \code{"error"}, or \code{"gain"}.}

\item{treatment}{Binary treatment column. Required when \code{target = "cate"}.}

\item{model}{Candidate models for the proxy. The winner is chosen by how well
it \emph{detects} heterogeneity (see \code{\link[=learners]{learners()}}), not by prediction error.}

\item{estimator}{Backend for the proxy models. See \code{\link[=fit_ml]{fit_ml()}}.}

\item{time}{Optional ordering column.}

\item{num_splits}{Number of random splits. More splits, less seed-dependence.}

\item{prop_aux}{Share of the sample used to learn the proxy in each split.}

\item{n_groups}{Number of sorted groups.}

\item{clan}{Person-level columns to describe the extreme groups with. Defaults
to the predictors.}

\item{split}{\code{"person"} (persons are the independent units) or \code{"occasion"}
(split rows within each person). \code{gain} requires \code{"occasion"}, since a
person needs rows in both halves to have a model of their own.}

\item{conf_level}{Confidence level.}

\item{...}{Passed to the proxy models.}
}
\value{
An \code{idiographic_heterogeneity} object. See \code{\link[=heterogeneity]{heterogeneity()}}, \code{\link[=clan]{clan()}},
and \code{\link[=learners]{learners()}}.
}
\description{
Estimates a person-varying quantity, tests whether it genuinely varies, sorts
people by it, and describes who sits at the extremes.
}
\details{
What varies is chosen by \code{target}:
\describe{
\item{\code{cate}}{How much a \code{treatment} helped. Needs \code{treatment}.}
\item{\code{error}}{How predictable each person is. Sorted groups run from the
people the model serves best to the people it fails.}
\item{\code{gain}}{How much a person-specific model beats a pooled one for this
person -- for whom idiographic modelling actually pays off. Positive means
modelling them alone helped.}
}

The reported quantities, for any target:
\describe{
\item{\code{average}}{The quantity, averaged over everyone. For \code{cate} this is
the ATE.}
\item{\code{group:g1..gK}}{Sorted groups, from the lowest value of the quantity
to the highest.}
\item{\code{group:top-bottom}}{The gap between the extremes. If its interval
excludes zero, the quantity genuinely differs across people.}
\item{\code{heterogeneity}}{The slope of the held-out scores on the proxy. A
significant slope means the variation is real and predictable, not noise.}
}

Inference follows the paper: the data are split many times, each split is
analysed separately, and results are aggregated by median with a conservative
interval and a doubled p-value. A single split is seed-dependent, which is the
problem this design exists to solve. Standard errors are clustered on the
person throughout.

Note that these splits are \emph{random}, not time-ordered. The question here is
whether a quantity varies across people, not whether the future can be
forecast; \code{\link[=fit_rolling]{fit_rolling()}} is the verb for the latter.
}
\examples{
set.seed(1)
d <- data.frame(id = rep(1:10, each = 30), day = rep(1:30, 10),
                x1 = rnorm(300), x2 = rnorm(300))
d$drug <- rbinom(300, 1, 0.5)
d$mood <- 2 * d$drug * (d$x1 > 0) + 0.5 * d$x1 + rnorm(300, sd = 0.5)

het <- fit_heterogeneity(d, y = "mood", x = c("x1", "x2"), id = "id",
                         target = "cate", treatment = "drug",
                         num_splits = 20)
heterogeneity(het)
clan(het)
}
\references{
Chernozhukov, V., Demirer, M., Duflo, E., & Fernandez-Val, I.
(2020). Generic Machine Learning Inference on Heterogeneous Treatment
Effects in Randomized Experiments. arXiv:1712.04802.
}
