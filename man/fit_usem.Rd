% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usem.R
\name{fit_usem}
\alias{fit_usem}
\title{Build a user-specified unified SEM network}
\usage{
fit_usem(
  data,
  vars,
  id,
  time = NULL,
  day = NULL,
  beep = NULL,
  min_obs = NULL,
  subject = NULL,
  temporal = c("ar", "all", "none"),
  contemporaneous = c("none", "all"),
  residual_cov = TRUE,
  trim = FALSE,
  trim_alpha = 0.05,
  trim_fit_criteria = 3L,
  cfi_cutoff = 0.95,
  tli_cutoff = 0.95,
  rmsea_cutoff = 0.08,
  srmr_cutoff = 0.08,
  paths = NULL,
  exogenous = NULL,
  standardize = FALSE,
  estimator = "ml",
  seed = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} in long format.}

\item{vars}{Character vector of time-varying variables.}

\item{id}{Character string naming the person-ID column.}

\item{time}{Character string naming the within-person ordering column, or
\code{NULL}.}

\item{day}{Character string naming the day/session column, or \code{NULL}. When
supplied, lag pairs are formed only within the same \verb{(id, day)} block.}

\item{beep}{Character string naming the measurement-occasion column, or
\code{NULL}. Used with \code{day} to order observations when \code{time} is not supplied.}

\item{min_obs}{Integer or \code{NULL}. Keep only subjects with at least this many
observations.}

\item{subject}{Optional vector naming the subject(s) to analyse.}

\item{temporal}{\code{"ar"} (own-lag only; default), \code{"all"} (all lagged
predictors), \code{"none"}, or a character vector of lavaan regressions such as
\code{"A ~ Blag"}.}

\item{contemporaneous}{\code{"none"} (default), \code{"all"} (all directed lag-0
predictors except self-regressions), or lavaan regressions such as
\code{"B ~ A"}.}

\item{residual_cov}{Logical. Estimate residual covariances among current
endogenous variables? Default \code{TRUE}.}

\item{trim}{Logical. If \code{TRUE}, treat \code{temporal}, \code{contemporaneous}, and
\code{residual_cov} as an eligible search space: start from the structural
baseline, add paths by modification index until fit criteria are met, then
prune weak paths. This is an idiographic clean-room search layer, not a clone of
any external package. Default \code{FALSE} fits the exact fixed syntax.}

\item{trim_alpha}{Significance level used for modification-index entry and
z-value pruning when \code{trim = TRUE}. Default \code{0.05}.}

\item{trim_fit_criteria}{Number of fit criteria that must pass before
forward search stops. Default \code{3}.}

\item{cfi_cutoff, tli_cutoff, rmsea_cutoff, srmr_cutoff}{Fit thresholds used by
trimmed uSEM.}

\item{paths}{Extra lavaan syntax lines to include unchanged.}

\item{exogenous}{Optional subset of \code{vars} to treat as exogenous current
variables. They can predict endogenous variables but are not outcomes.}

\item{standardize}{Logical. Standardize variables per person before fitting.}

\item{estimator}{Lavaan estimator. Default \code{"ml"}.}

\item{seed}{Optional random seed.}
}
\value{
A \code{net_usem} object with average \verb{$temporal},
\verb{$contemporaneous}, and \verb{$residual_cov} matrices, per-subject matrices in
\verb{$subjects}, a tidy coefficient table from \code{\link[=coefs]{coefs()}}, fit indices, syntax,
labels, and configuration metadata.
}
\description{
Fits person-specific unified Structural Equation Models (uSEM) for intensive
longitudinal data. A uSEM combines lagged directed effects, optional
contemporaneous directed effects, and optional residual covariances in one
SEM. Unlike \code{\link[=fit_gimme]{fit_gimme()}}, this function does no automated path search:
the model is fixed by \code{temporal}, \code{contemporaneous}, \code{residual_cov}, and
\code{paths}. With \code{trim = TRUE}, idiographic uses an independent clean-room
modification-index entry and z-value pruning layer over the declared
candidate set.
}
\examples{
\dontshow{if (requireNamespace("lavaan", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(1)
d <- data.frame(
  id = rep(1:4, each = 30),
  t = rep(seq_len(30), 4),
  A = rnorm(120), B = rnorm(120), C = rnorm(120)
)
fit <- fit_usem(d, vars = c("A", "B", "C"), id = "id", time = "t")
edges(fit)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=fit_gimme]{fit_gimme()}}, \code{\link[=fit_graphical_var]{fit_graphical_var()}}, \code{\link[=fit_mlvar]{fit_mlvar()}}
}
