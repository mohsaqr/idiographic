% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_pooling.R
\name{pool_coefs}
\alias{pool_coefs}
\title{Pool person-specific coefficients, separating real spread from noise}
\usage{
pool_coefs(
  x,
  term = NULL,
  scope = "individual",
  model = NULL,
  subgroup = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{x}{An \code{idiostats_fit} with individual-scope coefficients.}

\item{term}{Optional filter on the coefficient name.}

\item{scope}{Which scope's coefficients to pool. Individual, necessarily.}

\item{model, subgroup}{Optional filters.}

\item{conf_level}{Confidence level for the pooled interval.}
}
\value{
A \code{data.frame} of one row per term, with class
\code{idiostats_pooled}.
}
\description{
Runs a random-effects meta-analysis across people, one per term, treating
each person as a study with an estimate and a standard error.
}
\details{
The comparison to read first is \strong{\code{sd_observed} against \code{tau}}. The former
is the spread you can see in the person-specific coefficients; the latter is
how much of it is real once each person's own estimation error is taken out.
If they are close, people genuinely differ. If \code{tau} is much the smaller,
most of the apparent heterogeneity is measurement noise from short series,
and any subgroups found by clustering those coefficients are partly an
artefact of that noise.

\describe{
\item{\code{estimate}}{Random-effects pooled effect, with \code{tau} in its weights,
so people are not weighted purely by how much data they happen to have.}
\item{\code{tau}}{Estimated standard deviation of the \emph{true} person effects
(DerSimonian-Laird). Zero means the people are indistinguishable once
noise is accounted for.}
\item{\code{i2}}{Share of observed variation that is real rather than sampling
error, from 0 to 1.}
\item{\code{q}, \code{q_p}}{Cochran's Q and its p-value: is there \emph{any} real
heterogeneity at all?}
}

Intervals use the \strong{Hartung-Knapp} variance with \code{t} on \code{k - 1} degrees of
freedom, \code{k} being the number of people. The textbook random-effects
standard error treats \code{tau} as known when it was estimated, and under-covers
as a result: with 10 people it gave 0.930 coverage in simulation where 0.95
was claimed, against 0.945 for the adjustment used here. The adjustment can
only ever widen the interval.
}
\section{Read \code{q_p} with caution}{

Cochran's Q assumes each person's standard error is \emph{known}. Here it is
estimated from that person's own handful of occasions, which inflates Q. In
simulation on data with \strong{no} real heterogeneity at all, \code{q_p} fell below
0.05 in \strong{13\%} of runs rather than 5\% -- a false-positive rate roughly
2.6 times what it claims, and it did not improve when each person had 60
occasions instead of 25.

So treat a small \code{q_p} as suggestive, not decisive, and read the \emph{size} of
\code{tau} against \code{sd_observed} instead. Those are well behaved: across the same
simulations \code{tau} recovered true values of 0, 0.30 and 0.60 as 0.06, 0.30
and 0.59, and the pooled estimate was unbiased throughout.

Needs coefficients that carry standard errors, so it works on \code{\link[=fit_lm]{fit_lm()}},
\code{\link[=fit_glm]{fit_glm()}} and \code{\link[=fit_within_between]{fit_within_between()}} results. \code{\link[=fit_ml]{fit_ml()}} does not report
them and is refused rather than silently pooled as if every person were
measured equally well.
}

\examples{
fit <- fit_lm(srl, y = "effort", x = c("efficacy", "planning"),
              id = "name", time = "day", scope = "individual")
pool_coefs(fit)
}
