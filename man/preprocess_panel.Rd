% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_preprocess.R
\name{preprocess_panel}
\alias{preprocess_panel}
\title{Prepare repeated-measures data for idiographic modelling}
\usage{
preprocess_panel(
  data,
  id,
  time = NULL,
  vars = NULL,
  center = c("none", "person", "grand"),
  scale = c("none", "person", "grand"),
  lag = NULL,
  decompose = FALSE,
  lag_max_gap = NULL,
  detrend = c("none", "person", "grand"),
  detrend_alpha = 0.05
)
}
\arguments{
\item{data}{Data frame.}

\item{id}{Person/unit ID column.}

\item{time}{Optional ordering column. Required for \code{lag}.}

\item{vars}{Columns to transform (any \code{\link[=fit_lm]{fit_lm()}} selector). Defaults to every
numeric column other than \code{id} and \code{time}.}

\item{center}{\code{"none"}, \code{"person"}, or \code{"grand"}.}

\item{scale}{\code{"none"}, \code{"person"}, or \code{"grand"}.}

\item{lag}{Integer lags to create, e.g. \code{lag = 1} or \code{lag = 1:2}.}

\item{decompose}{Append \verb{<var>_within} and \verb{<var>_between} columns.}

\item{lag_max_gap}{Largest elapsed time a lag may span, on the scale of
\code{time}. A lagged value reaching further back becomes \code{NA}. \code{NULL} accepts
any gap, which is only safe when occasions are evenly spaced.}

\item{detrend}{\code{"none"}, \code{"person"} (each person's own trend over their own
occasions), or \code{"grand"} (one trend across everyone). Needs \code{time}.}

\item{detrend_alpha}{Only remove a trend when it is significant at this
level. Set to \code{1} to detrend unconditionally.}
}
\value{
The data frame, transformed in place, with any lag and decomposition
columns appended.
}
\description{
Three transforms that idiographic work almost always needs, in one call.
}
\details{
\strong{Person-centering} (\code{center = "person"}) subtracts each person's own mean,
so what remains is how a person varies \emph{around themselves}. This is the
transform that separates a within-person effect from a between-person one: on
raw scores a predictor can look strong only because high-scoring people also
score high on the outcome, while within any single person it does nothing.
Grand-centering (\code{center = "grand"}) subtracts one overall mean and does not
remove that confound.

\strong{Person-scaling} (\code{scale = "person"}) divides by each person's own standard
deviation, putting people on a common footing when they differ in how much
they fluctuate.

\strong{Lagging} (\code{lag}) adds \verb{<var>_lag1}-style columns, shifted \emph{within} each
person and in time order, so a person's yesterday never leaks into another
person's today. Rows with no predecessor get \code{NA}; the fitters drop them.

A lag counts \strong{occasions, not elapsed time}. When sampling is irregular
that is a real hazard: "the previous occasion" may be twenty minutes back
for one row and three days back for the next, and a lag-1 effect estimated
across both is not one quantity. Give \code{lag_max_gap} to refuse the stretched
ones -- a lagged value whose actual gap exceeds it becomes \code{NA} rather than
a silently incomparable number. \code{\link[=describe_persons]{describe_persons()}} reports \code{gap_median}
and \code{gap_max} so you can see whether this applies to your data before
choosing.

\strong{Detrending} (\code{detrend}) removes a linear time trend, so that what remains
is fluctuation around a person's trajectory rather than the trajectory
itself. Two people can both be rising steadily and appear strongly
correlated on every variable purely because time is passing. Following the
usual experience-sampling convention the trend is removed \strong{only when it is
significant} (\code{detrend_alpha = 0.05}); set \code{detrend_alpha = 1} to remove it
unconditionally. \code{detrend = "person"} fits each person's own trend over
their own occasions -- the idiographic choice, and the default meaning of
detrending here -- while \code{"grand"} fits a single trend across everyone.

Detrending preserves each series' \strong{level}: the residuals have the mean
added back, so \code{detrend} removes only the slope and stays independent of
\code{center}. Series with fewer than three usable occasions are left alone.

\strong{Decomposing} (\code{decompose = TRUE}) keeps \emph{both} halves instead of choosing
one: it appends \verb{<var>_within} (the deviation from the person's own mean) and
\verb{<var>_between} (that mean), leaving the original column untouched. This is
the input a within-between model needs -- see \code{\link[=fit_within_between]{fit_within_between()}}, which
does the same split internally on training rows only. Person-centering and
decomposition are alternatives, not companions: centering throws the between
half away, so asking for both at once is refused.
}
\examples{
# Within-person variation only, plus yesterday's efficacy.
prepped <- preprocess_panel(srl, id = "name", time = "day",
                      vars = "efficacy:monitoring",
                      center = "person", lag = 1)
fit_lm(prepped, y = "effort", x = c("efficacy", "efficacy_lag1"),
       id = "name", time = "day")

# Keep both halves, so the two effects can be compared in one model. A
# between-person column is constant inside a person, so it only belongs in a
# pooled model -- or use fit_within_between(), which handles the split itself.
split <- preprocess_panel(srl, id = "name", vars = c("efficacy", "planning"),
                    decompose = TRUE)
fit_lm(split, y = "effort", x = c("efficacy_within", "efficacy_between"),
       id = "name", scope = "pooled")
}
