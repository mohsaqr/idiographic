% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_within_between.R
\name{contextual}
\alias{contextual}
\title{Compare the within-person and between-person effect of each predictor}
\usage{
contextual(
  x,
  variable = NULL,
  scope = NULL,
  subject = NULL,
  subgroup = NULL,
  sort_by = NULL,
  decreasing = FALSE,
  n = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{\link[=fit_within_between]{fit_within_between()}} result.}

\item{variable, scope, subject, subgroup}{Optional filters.}

\item{sort_by}{Optional column to sort by.}

\item{decreasing}{Sort order when \code{sort_by} is supplied.}

\item{n}{Optional number of rows.}

\item{...}{Ignored.}
}
\value{
A data frame with one row per predictor per unit.
}
\description{
The point of a within-between model. \code{within} is the effect of a person
moving away from their own average; \code{between} is the effect of one person
averaging higher than another. \code{contextual} is \code{between - within}, with a
person-clustered test: if its interval excludes zero, the two processes are
genuinely different and pooling them into one coefficient is a modelling
error.
}
\details{
\code{model = "within"} and \code{model = "between"} fit only one component, so their
contextual effect is \code{NA} -- there is nothing to compare it against.
}
\examples{
fit <- fit_within_between(srl, y = "effort", x = c("efficacy", "planning"),
                          id = "name", time = "day")
contextual(fit)
}
