% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{equivalence}
\alias{equivalence}
\title{Report method-equivalence evidence}
\usage{
equivalence(x)
}
\arguments{
\item{x}{A fitted object.}
}
\value{
An \code{idiographic_equivalence} list with \code{method}, \code{status},
\code{reference}, \code{scope}, \code{tolerance}, \code{notes}, and \code{source}.
}
\description{
Returns the equivalence declaration attached by \code{\link[=fit_idiographic]{fit_idiographic()}}. For a
result created by a direct \verb{fit_*()} call, the registry can infer the method
from a unique registered result class. The declaration describes the scope
of committed validation; it is not a new statistical equivalence test.
}
\examples{
set.seed(2)
d <- data.frame(A = rnorm(40), B = rnorm(40))
fit <- fit_var(d, vars = c("A", "B"), scale = FALSE)
equivalence(fit)
}
