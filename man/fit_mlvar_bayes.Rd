% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlvar_bayes.R
\name{fit_mlvar_bayes}
\alias{fit_mlvar_bayes}
\title{Build a Bayesian multilevel VAR network (Mplus DSEM-targeted)}
\usage{
fit_mlvar_bayes(
  data,
  vars,
  id,
  day = NULL,
  beep = NULL,
  lags = 1L,
  temporal = c("fixed", "default", "random"),
  contemporaneous = c("fixed", "default"),
  residual = c("fixed", "random"),
  scale = TRUE,
  scaleWithin = FALSE,
  tinterval = NULL,
  impute = FALSE,
  n_iter = 4000L,
  n_burnin = NULL,
  n_chains = 2L,
  thin = 1L,
  seed = NULL,
  min_obs = NULL,
  subject = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame} containing the panel data.}

\item{vars}{Character vector of variable column names to model (length >= 2).}

\item{id}{Character string naming the person-ID column.}

\item{day}{Character string naming the day/session column, or \code{NULL}.}

\item{beep}{Character string naming the measurement-occasion column, or
\code{NULL}. When \code{NULL}, row position within each (id, day) block is used.}

\item{lags}{Integer lag order; only \code{1} is supported (matches Mplus DSEM
defaults here).}

\item{temporal}{Character. \code{"fixed"} (default) fits fixed temporal effects
with random intercepts (Mplus DSEM \code{temporal = "fixed"}). \code{"random"} fits
the full DSEM with person-specific temporal matrices \code{B_i} and a full
random-effect covariance over \verb{(mu_i, vec(B_i))}; the temporal network then
reports the posterior mean transition matrix and \code{attr(fit, "slope_sd")}
holds the per-coefficient random-slope SDs. \code{"random"} needs more subjects
estimable random-effect covariance: at least \code{2 * (p + p^2) + 1} subjects.}

\item{contemporaneous}{Character. Only \code{"fixed"} is implemented.}

\item{residual}{Character. \code{"fixed"} (default) uses one shared population
within-person residual covariance. \code{"random"} (only with
\code{temporal = "random"}) gives each subject their own residual covariance
\code{Sigma_W_i} via a conjugate hierarchical inverse-Wishart
(\code{Sigma_W_i ~ IW(Lambda, p + 2)}, \code{Lambda ~ Wishart}), matching DSEM
person-specific innovation variances; the reported contemporaneous network
is then the population-average residual covariance.}

\item{scale}{Logical. Global grand-mean/SD standardization of each variable
before fitting (Mplus/\code{mlVAR} \code{scale = TRUE}). Default \code{TRUE}.}

\item{scaleWithin}{Logical. Additionally within-person scale each variable.
Default \code{FALSE}.}

\item{tinterval}{Numeric or \code{NULL}. When supplied, \code{beep} is treated as a
continuous time variable and binned onto a regular grid of this width
(Mplus \code{TINTERVAL}); the integer bin becomes the occasion index for
gap-aware lagging, and multiple observations in one (id, day, bin) slot are
collapsed to the first. Lagging is gap-aware in all cases: lag-1 pairs are
only formed between consecutive occasions, so missing occasions never
create spurious lag pairs. Default \code{NULL}.}

\item{impute}{Logical. If \code{TRUE} (only with \code{temporal = "random"}), missing
observations are imputed \strong{within the model} each MCMC iteration (data
augmentation), rather than dropped: each person's series is expanded to a
full occasion grid and every latent cell is drawn from its Gaussian full
conditional (as an outcome at \code{t} and a predictor at \code{t+1}), using a
vectorised even/odd (checkerboard) block sweep. This matches how Mplus /
Stan / JAGS handle missing data and removes the listwise-deletion bias under
heavy missingness, at extra computational cost. Default \code{FALSE}.}

\item{n_iter}{Integer. Total MCMC iterations per chain. Default \code{4000}.}

\item{n_burnin}{Integer. Burn-in iterations discarded per chain. Default
\code{n_iter / 2} (Mplus's first-half burn-in convention).}

\item{n_chains}{Integer. Number of independent chains. Default \code{2}.}

\item{thin}{Integer. Keep every \code{thin}-th post-burn-in draw. Default \code{1}.}

\item{seed}{Integer or \code{NULL}. Base RNG seed (chain \code{c} uses \code{seed + c}).}

\item{min_obs}{Integer or \code{NULL}. Keep only subjects with at least this many
observations before fitting.}

\item{subject}{Optional vector naming the exact subject(s) to analyse.}

\item{verbose}{Logical. Emit progress messages. Default \code{FALSE}.}
}
\value{
A \code{net_mlvar_bayes} object (also inheriting \code{net_mlvar}), a named
list of three netobjects (\code{temporal}, \code{contemporaneous}, \code{between}) with
posterior-summary attributes. \code{coefs()} returns a tidy table with
\code{estimate} (posterior median), \code{posterior_sd}, \code{ci_lower}, \code{ci_upper},
\code{p} (one-tailed), and \code{significant} (95\% CI excludes 0). Posterior draws
and the max Gelman-Rubin PSR are kept in attributes.
}
\description{
Native, pure-R Bayesian estimator for a two-level VAR(1) that
statistically reproduces Mplus DSEM output (the estimator behind
\code{mlVAR::mlVAR(estimator = "Mplus")}) without needing Mplus installed. A
conjugate Gibbs sampler estimates a fixed temporal matrix, a within-person
residual (contemporaneous) network, and a between-person network, using
latent mean centering and Mplus's default priors. Point estimates are
posterior medians with posterior SDs and 95\% credible intervals.
}
\details{
The sampler alternates five conjugate full-conditional draws per
iteration: the latent person means \code{mu_i} (Gaussian), the fixed temporal
matrix \code{B} (matrix-normal), the within residual covariance \code{Sigma_W}
(inverse-Wishart), the grand mean \code{alpha} (Gaussian), and the between
covariance \code{Sigma_B} (inverse-Wishart). The lagged predictor is recentred
on the current \code{mu_i} draw every iteration (latent mean centering). Data
are globally standardized first (matching \code{mlVAR}'s \code{scale = TRUE}); the
first observation of each block is used only as a lag (condition-on-first).

Validated to statistical (Monte-Carlo-error) equivalence against real
Mplus 9 DSEM output on standardized synthetic panels: posterior medians of
\code{B}, \code{Sigma_W}, \code{Sigma_B} agree with Mplus to well within a posterior SD.
}
\examples{
\donttest{
set.seed(1)
n_id <- 10; n_t <- 40; vars <- c("A", "B")
rows <- lapply(seq_len(n_id), function(i) {
  y <- matrix(0, n_t, 2)
  for (t in 2:n_t) y[t, ] <- c(0.3, 0.15) * y[t - 1, ] + rnorm(2)
  data.frame(id = i, beep = seq_len(n_t), A = y[, 1], B = y[, 2])
})
d <- do.call(rbind, rows)
fit <- fit_mlvar_bayes(d, vars = vars, id = "id", beep = "beep",
                         n_iter = 2000, seed = 1)
print(fit)
coefs(fit)
}
}
\seealso{
\code{\link[=fit_mlvar]{fit_mlvar()}} (frequentist lmer path), \code{\link[=fit_mlvar_mplus]{fit_mlvar_mplus()}}
(true-Mplus wrapper).
}
