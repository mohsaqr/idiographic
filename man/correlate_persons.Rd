% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_describe.R
\name{correlate_persons}
\alias{correlate_persons}
\title{Correlate variables within each person}
\usage{
correlate_persons(
  data,
  id,
  vars = NULL,
  time = NULL,
  subject = NULL,
  variable = NULL,
  sort_by = NULL,
  decreasing = FALSE,
  n = NULL,
  conf_level = 0.95,
  min_n = 4L
)
}
\arguments{
\item{data}{Data frame of repeated measures.}

\item{id}{Person/unit ID column.}

\item{vars}{Columns to correlate (any \code{\link[=fit_lm]{fit_lm()}} selector). Defaults to every
numeric column other than \code{id} and \code{time}.}

\item{time}{Optional ordering column, excluded from \code{vars} by default.}

\item{subject}{Optional person(s) to correlate. Defaults to everyone.}

\item{variable}{Optional variable(s); keeps pairs involving any of them.}

\item{sort_by}{Optional column of the result to sort by, e.g. \code{"r"}.}

\item{decreasing}{Sort order when \code{sort_by} is supplied.}

\item{n}{Optional number of rows to keep.}

\item{conf_level}{Confidence level for the intervals.}

\item{min_n}{Fewest complete pairs a person needs before a correlation is
reported rather than \code{NA}.}
}
\value{
A \code{data.frame} of one row per person per pair, with class
\code{idiostats_correlations}.
}
\description{
The most common idiographic statistic there is: for each person separately,
how do two variables move together \emph{within} that person. One row per person
per pair.
}
\details{
A correlation is unchanged by shifting a variable's location, so a
person-specific correlation is already a \strong{within-person} correlation --
centering the data first would not change these numbers. What it is not is
the pooled correlation, which mixes within- and between-person covariation
and can carry the opposite sign.

Confidence intervals and p-values come from the Fisher z transform, on that
person's own usable pairs.
}
\examples{
correlate_persons(srl, "name", vars = c("effort", "efficacy", "planning"))

# The strongest person-specific associations, sorted.
correlate_persons(srl, "name", vars = c("effort", "efficacy"),
                  sort_by = "r", decreasing = TRUE, n = 5)
}
