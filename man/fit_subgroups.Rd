% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_subgroups.R
\name{fit_subgroups}
\alias{fit_subgroups}
\title{Fit subgroup-specific models}
\usage{
fit_subgroups(
  data,
  y,
  x,
  id,
  subgroup,
  method = c("lm", "glm", "ml"),
  scope = "all",
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{y}{Outcome column name.}

\item{x}{Predictors: names, numeric positions, \code{a:b} range, formula, or data frame.}

\item{id}{Person/unit ID column.}

\item{subgroup}{Subgroup mapping: an \code{\link[=find_subgroups]{find_subgroups()}} result, a grouping
column in \code{data}, or a named vector of labels per person.}

\item{method}{Which family to fit: \code{"lm"}, \code{"glm"}, or \code{"ml"}.}

\item{scope}{Defaults to \code{"all"} (pooled + subgroup + individual).}

\item{...}{Passed to the underlying fitter, e.g. \code{model} or \code{family}.}
}
\value{
An \code{idiostats_fit}.
}
\description{
Fits pooled, subgroup and person-specific models together so all three levels
can be compared in one table.
}
\examples{
g <- find_subgroups(srl, y = "effort", x = "efficacy:monitoring",
                    id = "name", k = 2, reps = 10)
fit <- fit_subgroups(srl, y = "effort", x = "efficacy:monitoring",
                     id = "name", subgroup = g, time = "day")
metrics(fit, overall = TRUE)
}
