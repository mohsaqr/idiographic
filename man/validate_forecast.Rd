% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{validate_forecast}
\alias{validate_forecast}
\title{Validate one-step forecasts from idiographic VAR models (experimental)}
\usage{
validate_forecast(
  data,
  vars,
  estimator = c("var", "graphical_var"),
  id = NULL,
  day = NULL,
  beep = NULL,
  initial = NULL,
  assess = 1L,
  step = 1L,
  n_splits = NULL,
  block_size = NULL,
  scale = TRUE,
  center_within = TRUE,
  delete_missings = TRUE,
  keep_fits = FALSE,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or matrix with columns for variables and optional
id/day/beep columns.}

\item{vars}{Character vector of variable names.}

\item{estimator}{\code{"var"} (default) for \code{\link[=fit_var]{fit_var()}} or \code{"graphical_var"}
for \code{\link[=fit_graphical_var]{fit_graphical_var()}}.}

\item{id}{Character. Name of the person-ID column, or \code{NULL}.}

\item{day}{Character. Name of the day/session column, or \code{NULL}.}

\item{beep}{Character. Name of the measurement-occasion column, or \code{NULL}.}

\item{initial}{Integer number of ordered blocks in the first training split.
Default uses 60 percent of blocks, leaving at least one assessment block.}

\item{assess}{Integer number of blocks to assess per split. Default \code{1}.}

\item{step}{Integer number of blocks to advance between splits. Default \code{1}.}

\item{n_splits}{Optional maximum number of rolling splits.}

\item{block_size}{Integer or \code{NULL}. Consecutive block length used only when
neither \code{id} nor \code{day} is supplied. Defaults to \code{floor(sqrt(nrow(data)))}.}

\item{scale}{Logical. Whether to standardize using training-split means and
SDs. Default \code{TRUE}.}

\item{center_within}{Logical. Whether to centre within person using
training-split person means when more than one id is present. Default
\code{TRUE}.}

\item{delete_missings}{Logical. Drop incomplete current/lagged assessment
rows. Default \code{TRUE}.}

\item{keep_fits}{Logical. Store fitted split models? Default \code{FALSE}.}

\item{...}{Further arguments passed to the estimator.}
}
\value{
A \code{forecast_result} with \verb{$predictions}, \verb{$metrics}, \verb{$splits},
\verb{$failures}, and optionally \verb{$fits}.
}
\description{
\strong{Experimental.} The rolling-origin design follows standard time-series
cross-validation practice, but unlike the estimators in this package it has
no external reference implementation to validate against, and its
interface, defaults, and reported metrics may change in a future release.

Performs rolling-origin one-step prediction from \code{\link[=fit_var]{fit_var()}} or
\code{\link[=fit_graphical_var]{fit_graphical_var()}}. Each split fits the estimator on earlier blocks and
predicts current variables in the next block from their lag-1 values. Scaling
and within-person centring parameters are learned from the training split
only, then applied to the assessment split before prediction.
}
\examples{
set.seed(1)
d <- data.frame(id = 1, day = rep(1:5, each = 12),
                beep = rep(1:12, 5),
                A = rnorm(60), B = rnorm(60), C = rnorm(60))
fc <- validate_forecast(d, vars = c("A", "B", "C"), id = "id",
                        day = "day", beep = "beep",
                        initial = 3, n_splits = 2, scale = FALSE)
fc$metrics
}
