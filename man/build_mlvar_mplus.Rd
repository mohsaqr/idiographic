% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlvar_mplus.R
\name{build_mlvar_mplus}
\alias{build_mlvar_mplus}
\title{Build an Mplus-backed multilevel VAR network}
\usage{
build_mlvar_mplus(
  data,
  vars,
  id,
  day = NULL,
  beep = NULL,
  lags = 1L,
  temporal = c("fixed", "correlated", "orthogonal", "default"),
  contemporaneous = c("fixed", "correlated", "orthogonal", "default"),
  nCores = 1L,
  scale = TRUE,
  scaleWithin = FALSE,
  MplusSave = TRUE,
  MplusName = "mlVAR_mplus",
  iterations = "(2000)",
  chains = nCores,
  signs,
  min_obs = NULL,
  subject = NULL,
  workdir = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} containing the panel data.}

\item{vars}{Character vector of variable column names to model.}

\item{id}{Character string naming the person-ID column.}

\item{day}{Character string naming the day/session column, or \code{NULL}.
Mplus estimation in \code{mlVAR} does not directly support \code{day}; when supplied
it is passed through so \code{mlVAR} can prepare the row order, but \code{mlVAR} will
warn about the Mplus limitation.}

\item{beep}{Character string naming the measurement-occasion column, or
\code{NULL}.}

\item{lags}{Integer lag order. The Mplus backend currently supports \code{1}.}

\item{temporal, contemporaneous}{Random-effect structure passed to \code{mlVAR}.
Supported Mplus values are \code{"fixed"}, \code{"correlated"}, \code{"orthogonal"}, and
\code{"default"}.}

\item{nCores}{Number of Mplus processors/chains.}

\item{scale, scaleWithin}{Standardization options passed to \code{mlVAR}.}

\item{MplusSave}{Logical. Keep Mplus input/output files in the working
directory? Default \code{TRUE}.}

\item{MplusName}{File stem for Mplus input/output files.}

\item{iterations}{Mplus \code{BITERATIONS} string, e.g. \code{"(2000)"}.}

\item{chains}{Number of Mplus chains. Defaults to \code{nCores}.}

\item{signs}{Optional sign matrix for contemporaneous random effects.}

\item{min_obs}{Integer or \code{NULL}. Keep only subjects with at least this many
observations before fitting.}

\item{subject}{Optional vector naming the exact subject(s) to analyse.}

\item{workdir}{Directory in which Mplus files should be written/run. Default
uses the current working directory.}

\item{verbose}{Logical. Show progress from \code{mlVAR}/Mplus.}

\item{...}{Additional arguments passed to \code{mlVAR::mlVAR()}.}
}
\value{
A \code{net_mplus} object, also inheriting from \code{net_mlvar}, with temporal,
contemporaneous, and between networks plus Mplus metadata in attributes.
The original \code{mlVAR}/Mplus object is available as \code{attr(x, "mplus")}.
}
\description{
Runs the Mplus Bayesian estimator exposed by \code{mlVAR::mlVAR(estimator = "Mplus")} and converts the returned posterior summaries into idiographic's
network/tidy accessors. This is a true Mplus backend: Mplus must be installed
and discoverable by \code{MplusAutomation::detectMplus()}.
}
\examples{
\dontshow{if (requireNamespace("mlVAR", quietly = TRUE) && requireNamespace("MplusAutomation", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontrun{
fit <- build_mlvar_mplus(
  data, vars = c("A", "B", "C"), id = "id", beep = "time",
  temporal = "fixed", contemporaneous = "fixed",
  MplusName = "my_mplus_mlvar"
)
edges(fit)
attr(fit, "mplus")$output$summaries
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=build_mlvar]{build_mlvar()}}
}
