% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gimme.R
\name{summary.net_gimme}
\alias{summary.net_gimme}
\title{Summary Method for net_gimme}
\usage{
\method{summary}{net_gimme}(object, ...)
}
\arguments{
\item{object}{A \code{net_gimme} object.}

\item{...}{Additional arguments (ignored).}
}
\value{
A tidy \code{data.frame} of per-network metrics (one row per network:
\code{temporal}, \code{contemporaneous}), with \code{n_edges}/\code{density}/etc. computed from
the proportion-of-subjects networks. Per-subject fit indices are in
\code{object$fit}; \code{coefs(object)} gives the per-person estimates,
\code{edges(object)} the tidy edge list, and \code{nodes(object)} node strengths.
}
\description{
Summary Method for net_gimme
}
\examples{
\dontshow{if (requireNamespace("lavaan", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(1)
panel <- data.frame(
  id = rep(1:5, each = 20),
  t  = rep(seq_len(20), 5),
  A  = rnorm(100), B = rnorm(100), C = rnorm(100)
)
gm <- fit_gimme(panel, vars = c("A","B","C"), id = "id", time = "t")
summary(gm)
}
\dontshow{\}) # examplesIf}
}
