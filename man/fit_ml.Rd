% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/idiographic_ml.R
\name{fit_ml}
\alias{fit_ml}
\alias{fit_idiographic_ml}
\alias{fit_individualized_ml}
\title{Fit idiographic supervised machine-learning models}
\usage{
fit_ml(
  data,
  outcome,
  predictors,
  id,
  day = NULL,
  beep = NULL,
  task = c("auto", "regression", "classification"),
  model = NULL,
  estimator = NULL,
  compare = c("both", "individual", "pooled"),
  test_prop = 0.2,
  min_train = 10L,
  min_test = 1L,
  lambda = 1,
  alpha = 0.5,
  k = 5L,
  n_components = NULL,
  max_iter = 100L,
  tol = 1e-06,
  standardize = TRUE,
  keep_fits = FALSE,
  ...
)

fit_idiographic_ml(
  data,
  outcome,
  predictors,
  id,
  day = NULL,
  beep = NULL,
  task = c("auto", "regression", "classification"),
  model = NULL,
  estimator = NULL,
  compare = c("both", "individual", "pooled"),
  test_prop = 0.2,
  min_train = 10L,
  min_test = 1L,
  lambda = 1,
  alpha = 0.5,
  k = 5L,
  n_components = NULL,
  max_iter = 100L,
  tol = 1e-06,
  standardize = TRUE,
  keep_fits = FALSE,
  ...
)

fit_individualized_ml(
  data,
  outcome,
  predictors,
  id,
  day = NULL,
  beep = NULL,
  task = c("auto", "regression", "classification"),
  model = NULL,
  estimator = NULL,
  compare = c("both", "individual", "pooled"),
  test_prop = 0.2,
  min_train = 10L,
  min_test = 1L,
  lambda = 1,
  alpha = 0.5,
  k = 5L,
  n_components = NULL,
  max_iter = 100L,
  tol = 1e-06,
  standardize = TRUE,
  keep_fits = FALSE,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or matrix.}

\item{outcome}{Character. Name of the outcome column.}

\item{predictors}{Character vector of predictor columns.}

\item{id}{Character. Name of the subject/person ID column.}

\item{day, beep}{Optional ordering columns. Rows are ordered by \code{id}, \code{day},
and \code{beep} before the last rows for each subject are held out.}

\item{task}{\code{"auto"}, \code{"regression"}, or \code{"classification"}. Auto treats a
numeric outcome as regression and a two-level non-numeric outcome as binary
classification.}

\item{model}{\code{NULL} for the task default, \code{"all"} for all native models for
the selected task, or a character vector of simple model names. Regression
models are \code{"mean"}, \code{"linear"}, \code{"ridge"}, \code{"lasso"}, \code{"elastic"},
\code{"pcr"}, \code{"knn"}, and \code{"tree"}. Classification models are \code{"majority"},
\code{"logistic"}, \code{"ridge"}, \code{"lasso"}, \code{"elastic"}, \code{"lda"}, \code{"bayes"},
\code{"knn"}, and \code{"tree"}.}

\item{estimator}{\code{NULL} for each model's default estimator, or a named
character vector/list mapping model names to estimator names. The native
base-R estimator is \code{"native"}. This is where package-specific backends
belong when the same model can be estimated more than one way.}

\item{compare}{Which models to fit: \code{"both"} (default), \code{"individual"}, or
\code{"pooled"}.}

\item{test_prop}{Proportion of each subject's ordered rows held out from the
end of the series. Default \code{0.2}.}

\item{min_train}{Minimum complete training rows required for a model. Default
\code{10}.}

\item{min_test}{Minimum held-out rows required per subject. Default \code{1}.}

\item{lambda}{Ridge penalty for \code{model = "ridge"}. The intercept is not
penalized. Also used by lasso and elastic-net models. Default \code{1}.}

\item{alpha}{Elastic-net mixing value in \verb{[0, 1]}; \code{0} is ridge and \code{1} is
lasso. Default \code{0.5}.}

\item{k}{Number of neighbours for \code{model = "knn"}. Default \code{5}.}

\item{n_components}{Number of principal components for \code{model = "pcr"}.
Default uses \code{min(5, n_predictors, n_train - 1)}.}

\item{max_iter}{Maximum iterations for coordinate-descent penalized models.
Default \code{100}.}

\item{tol}{Convergence tolerance for iterative models. Default \code{1e-6}.}

\item{standardize}{Logical. Standardize predictors using training-set means
and SDs? Default \code{TRUE}.}

\item{keep_fits}{Logical. Store fitted internal model objects? Default
\code{FALSE}.}

\item{...}{Optional model controls using the same names as the explicit
tuning arguments (\code{lambda}, \code{alpha}, \code{k}, \code{n_components}, \code{max_iter}, or
\code{tol}). Unknown names are rejected.}
}
\value{
An \code{idioml_result} with \verb{$predictions}, \verb{$metrics}, \verb{$coefficients},
\verb{$failures}, and optionally \verb{$fits}.
}
\description{
Fits train/test supervised prediction models in an idiographic design: each
subject can receive a model trained only on that subject's earlier rows, and
the same held-out rows can also be scored by a pooled model trained on all
subjects' earlier rows. This mirrors individualized modelling designs where
person-specific prediction is compared against a nomothetic pooled baseline.

The implementation is dependency-free beyond base R. Regression supports
mean baseline, ordinary least squares, ridge, lasso, elastic net, principal
component regression, k-nearest neighbours, and a one-split regression tree.
Binary classification supports majority baseline, logistic regression,
ridge/lasso/elastic-net logistic regression, linear discriminant analysis,
Gaussian naive Bayes, k-nearest neighbours, and a one-split classification
tree. Predictors are standardized using training rows only.
}
\examples{
set.seed(1)
d <- data.frame(
  id = rep(1:4, each = 40),
  beep = rep(seq_len(40), 4),
  x1 = rnorm(160),
  x2 = rnorm(160)
)
d$y <- 0.4 * d$x1 - 0.2 * d$x2 + rep(c(-1, 0, 1, 0.5), each = 40) +
  rnorm(160, sd = 0.4)
fit <- fit_ml(d, outcome = "y", predictors = c("x1", "x2"),
              id = "id", beep = "beep",
              model = c("linear", "ridge", "knn"))
fit$metrics
coefs(fit)
}
