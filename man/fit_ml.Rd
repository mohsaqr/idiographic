% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/idiographic_ml.R, R/stats_compat.R
\name{fit_idiographic_ml}
\alias{fit_idiographic_ml}
\alias{fit_individualized_ml}
\alias{fit_ml}
\title{Fit person-specific machine-learning models}
\usage{
fit_idiographic_ml(
  data,
  outcome,
  predictors,
  id,
  day = NULL,
  beep = NULL,
  task = c("auto", "regression", "classification"),
  model = NULL,
  estimator = NULL,
  compare = c("both", "individual", "pooled"),
  test_prop = 0.2,
  min_train = 10L,
  min_test = 1L,
  lambda = 1,
  alpha = 0.5,
  k = 5L,
  n_components = NULL,
  max_iter = 100L,
  tol = 1e-06,
  standardize = TRUE,
  keep_fits = FALSE,
  ...
)

fit_individualized_ml(
  data,
  outcome,
  predictors,
  id,
  day = NULL,
  beep = NULL,
  task = c("auto", "regression", "classification"),
  model = NULL,
  estimator = NULL,
  compare = c("both", "individual", "pooled"),
  test_prop = 0.2,
  min_train = 10L,
  min_test = 1L,
  lambda = 1,
  alpha = 0.5,
  k = 5L,
  n_components = NULL,
  max_iter = 100L,
  tol = 1e-06,
  standardize = TRUE,
  keep_fits = FALSE,
  ...
)

fit_ml(
  data,
  outcome,
  predictors,
  id,
  day = NULL,
  beep = NULL,
  task = c("auto", "regression", "classification"),
  model = NULL,
  estimator = NULL,
  compare = c("both", "individual", "pooled"),
  test_prop = 0.2,
  min_train = 10L,
  min_test = 1L,
  lambda = 1,
  alpha = 0.5,
  k = 5L,
  n_components = NULL,
  max_iter = 100L,
  tol = 1e-06,
  standardize = TRUE,
  keep_fits = FALSE,
  ...,
  y,
  x
)
}
\arguments{
\item{data}{A data frame or matrix.}

\item{outcome, predictors}{Historical \code{idiographic} outcome and predictor
arguments.}

\item{id}{Person identifier column.}

\item{day, beep}{Optional historical ordering columns.}

\item{task}{Outcome task: \code{"auto"}, \code{"regression"}, or \code{"classification"}.}

\item{model}{One or more model names.}

\item{estimator}{Optional implementation backend.}

\item{compare}{Historical scope selector: \code{"both"}, \code{"individual"}, or
\code{"pooled"}.}

\item{test_prop}{Proportion of each person's ordered rows used for testing.}

\item{min_train, min_test}{Minimum training and test rows.}

\item{lambda, alpha}{Penalized-model controls.}

\item{k}{Number of neighbours for nearest-neighbour models.}

\item{n_components}{Number of principal components for the historical API.}

\item{max_iter, tol}{Iteration limit and convergence tolerance.}

\item{standardize}{Use training-only predictor standardization?}

\item{keep_fits}{Retain fitted backend objects?}

\item{...}{Arguments passed to the selected implementation.}

\item{y, x}{Consolidated outcome and predictor selectors. Supply both.}
}
\value{
An \code{idioml_result} for historical calls or an \code{idiographic_fit} for
consolidated \code{y}/\code{x} calls.
}
\description{
\code{fit_ml()} supports both historical \code{idiographic} calls using
\code{outcome}/\code{predictors} and the consolidated panel-model API using \code{y}/\code{x}.
Calls that name \code{y} or \code{x} use the consolidated result contract; existing
positional and \code{outcome}/\code{predictors} calls retain their original behavior.
}
