% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_effects.R
\name{fit_effects}
\alias{fit_effects}
\title{Estimate treatment effects and their heterogeneity}
\usage{
fit_effects(
  data,
  y,
  treatment,
  x,
  id,
  time = NULL,
  scope = "both",
  subgroup = NULL,
  model = "auto",
  estimator = "native",
  treatment_type = "auto",
  reference = NULL,
  propensity = "auto",
  trim = 0.05,
  n_groups = 4L,
  conf_level = 0.95,
  test_prop = 0.3,
  min_train = 10L,
  min_test = 4L,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{y}{Outcome column name.}

\item{treatment}{Treatment column: two levels, several levels, or a dose.}

\item{x}{Predictors: names, numeric positions, \code{a:b} range, formula, or data frame.}

\item{id}{Person/unit ID column.}

\item{time}{Optional ordering column.}

\item{scope}{\code{"both"} (pooled + individual), \code{"pooled"}, \code{"individual"},
\code{"subgroup"}, or \code{"all"} (pooled + subgroup + individual).}

\item{subgroup}{Optional subgroup mapping: an \code{\link[=find_subgroups]{find_subgroups()}} result, a
grouping column in \code{data}, or a named vector of labels per person.}

\item{model}{Outcome model used for the response surfaces. Any model from
\code{\link[=fit_ml]{fit_ml()}}; \code{"auto"} picks the simple linear or logistic model for the
estimator in use.}

\item{estimator}{Backend for the outcome \emph{and} propensity models. See
\code{\link[=fit_ml]{fit_ml()}}.}

\item{treatment_type}{One of \code{"auto"}, \code{"binary"}, \code{"multiarm"},
\code{"continuous"}.}

\item{reference}{Arm to contrast against, for a multi-arm treatment.
Defaults to the first level.}

\item{propensity}{Model for the treatment given the predictors. \code{"auto"}
follows \code{estimator}. For a dose this is the \code{E[T|X]} regression.}

\item{trim}{Propensity values are held inside \verb{[trim, 1 - trim]} so a
near-zero denominator cannot dominate the score.}

\item{n_groups}{Number of sorted effect groups for GATES.}

\item{conf_level}{Confidence level for the reported intervals.}

\item{test_prop}{Proportion of each person's ordered rows held out.}

\item{min_train}{Minimum complete training rows per person.}

\item{min_test}{Minimum complete held-out rows per person.}

\item{...}{Passed to the outcome models, e.g. \code{lambda} or \code{k}.}
}
\value{
An \code{idiographic_effects} object, which is also an \code{idiographic_fit}: the
usual accessors work, and \code{\link[=effects]{effects()}} returns the effect table.
}
\description{
Answers a different question from the other fitters: not "how well can we
predict this person" but "how much did the treatment help, and for whom".
}
\details{
In repeated-measures data the treatment usually varies \emph{within} a person over
time, so a person-specific treatment effect is estimable. \code{fit_effects()}
reports one effect per scope: the pooled effect, the effect inside each
subgroup, and the effect for each individual.
}
\section{Treatment types}{

The treatment type is detected from the column and can be named outright
with \code{treatment_type}:
\describe{
\item{\code{binary}}{Two levels, or 0/1. Estimated by \strong{AIPW}: two response
surfaces and a propensity model are fitted on the training rows and
applied to the held-out rows to build one doubly robust score per row.
A wrong outcome model or a wrong propensity model can each be tolerated
-- not both. The reported effect is \code{ATE}.}
\item{\code{multiarm}}{Three or more unordered arms. The same AIPW score is
built for every arm, with a one-versus-rest propensity, and each arm is
contrasted against \code{reference}. One set of effects is reported per
contrast, distinguished by the \code{contrast} column.}
\item{\code{continuous}}{A dose. AIPW does not apply -- there are no two
surfaces to difference -- so the estimator is the partially linear
double-ML (Robinson) score: \code{E[Y|X]} and \code{E[T|X]} are fitted on the
training rows and the effect is the regression of one held-out residual
on the other. The reported effect is \code{APE}, the change in the outcome
per \strong{one unit} of treatment. \strong{Read the note below on what it
averages.}}
}
}

\section{What \code{APE} averages}{

If the dose effect is the same for everyone, \code{APE} is that effect. If it
varies, \code{APE} is \strong{not} the plain average \code{E[tau(X)]}: the partially linear
estimator returns the \emph{variance-weighted} average

\deqn{\frac{E[\mathrm{Var}(T \mid X)\,\tau(X)]}{E[\mathrm{Var}(T \mid X)]},}

which leans towards the people whose dose varies most, because they carry
the most information about its effect. The difference is not small: with
residualized doses of -1, 1, -3, 3 and true effects of 0, 0, 10, 10, the
plain average is 5 while this estimator returns 9.

This is the standard target of the partially linear model and it is the
right quantity for a summary. It is simply not the same question as "what
would the average person gain from one more unit". When the dose effect
varies -- and \code{GATES} in the same output will tell you whether it does --
report it as a projection, not as a population mean.
A numeric treatment with more than two distinct values is read as a dose.
Pass \code{treatment_type = "multiarm"} to treat its values as unordered arms
instead.

A two-level non-numeric outcome is modelled on the probability scale, so the
reported effect is a risk difference. A 0/1 numeric outcome is modelled as a
linear probability, which estimates the same risk difference.
}

\section{Reported effects}{

\describe{
\item{\code{ATE} / \code{APE}}{The average treatment effect, or for a dose the
average partial effect per unit.}
\item{\code{GATES:g1..gK}}{Sorted effect groups. Rows are ranked by their
predicted effect and cut into \code{n_groups} bins; \code{g1} is the least-helped
bin and \code{gK} the most-helped. \code{GATES:top-bottom} is their difference: if
its confidence interval excludes zero, the treatment genuinely helps some
more than others.}
\item{\code{BLP:heterogeneity}}{The slope of the held-out scores on the
predicted effect. A significant slope means the predicted heterogeneity
is real and not noise.}
}
}

\examples{
set.seed(1)
d <- data.frame(
  id = rep(1:6, each = 40), day = rep(1:40, 6),
  x1 = rnorm(240), x2 = rnorm(240)
)
d$drug <- rbinom(240, 1, 0.5)
# The drug helps people with a high x1 and does nothing for the rest.
d$mood <- 2 * d$drug * (d$x1 > 0) + 0.5 * d$x1 + rnorm(240, sd = 0.5)

fit <- fit_effects(d, y = "mood", treatment = "drug", x = c("x1", "x2"),
                   id = "id", time = "day", scope = "pooled")
effects(fit)

# A dose rather than a switch: the effect is reported per unit of dose.
d$dose <- round(runif(240, 0, 10), 1)
d$sleep <- 0.3 * d$dose + 0.5 * d$x1 + rnorm(240, sd = 0.5)
dose_fit <- fit_effects(d, y = "sleep", treatment = "dose",
                        x = c("x1", "x2"), id = "id", time = "day",
                        scope = "pooled")
effects(dose_fit, effect = "APE")
}
