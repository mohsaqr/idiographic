% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_fit_lm.R
\name{fit_lm}
\alias{fit_lm}
\title{Fit pooled, subgroup and person-specific linear models}
\usage{
fit_lm(
  data,
  y,
  x,
  id,
  time = NULL,
  scope = "both",
  subgroup = NULL,
  estimator = "native",
  weights = NULL,
  test_prop = 0.2,
  min_train = 10L,
  min_test = 1L,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{y}{Outcome column name.}

\item{x}{Predictors: names, numeric positions, \code{a:b} range, formula, or data frame.}

\item{id}{Person/unit ID column.}

\item{time}{Optional ordering column.}

\item{scope}{\code{"both"} (pooled + individual), \code{"pooled"}, \code{"individual"},
\code{"subgroup"}, or \code{"all"} (pooled + subgroup + individual).}

\item{subgroup}{Optional subgroup mapping: an \code{\link[=find_subgroups]{find_subgroups()}} result, a
grouping column in \code{data}, or a named vector of labels per person.}

\item{estimator}{\code{"native"} for \code{stats::lm()}, or \code{"robust"} for an
M-estimator (\code{MASS::rlm()}) that is not dragged around by outliers.}

\item{weights}{Optional column name in \code{data} holding case weights.}

\item{test_prop}{Proportion of each person's ordered rows held out.}

\item{min_train}{Minimum complete training rows per person.}

\item{min_test}{Minimum complete held-out rows per person.}

\item{...}{Passed to the underlying fitter.}
}
\value{
An \code{idiostats_fit}.
}
\description{
Fits a linear model at every requested scope and returns them in one tidy
object, so pooled and person-specific results are directly comparable.
}
\examples{
fit <- fit_lm(srl, y = "effort", x = "efficacy:monitoring", id = "name",
              time = "day")
metrics(fit, overall = TRUE)

# A few wild days should not decide a person's slope.
robust <- fit_lm(srl, y = "effort", x = "efficacy:monitoring", id = "name",
                 time = "day", estimator = "robust")
metrics(robust, overall = TRUE)
}
