% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/var.R
\name{fit_var}
\alias{fit_var}
\title{Build an ordinary least-squares VAR network}
\usage{
fit_var(
  data,
  vars,
  id = NULL,
  day = NULL,
  beep = NULL,
  lags = 1L,
  scale = TRUE,
  center_within = TRUE,
  delete_missings = TRUE,
  min_obs = NULL,
  subject = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} or matrix with columns for variables and optional
id/day/beep columns.}

\item{vars}{Character vector of variable names.}

\item{id}{Character. Name of the person-ID column, or \code{NULL} for a single
series.}

\item{day}{Character. Name of the day/session column, or \code{NULL}.}

\item{beep}{Character. Name of the measurement-occasion column, or \code{NULL}.}

\item{lags}{Integer. Only \code{1} is supported.}

\item{scale}{Logical. Whether to standardize variables before lagging.
Default \code{TRUE}.}

\item{center_within}{Logical. Whether to center within person when more than
one id is present. Default \code{TRUE}.}

\item{delete_missings}{Logical. Drop incomplete current/lagged rows. Default
\code{TRUE}.}

\item{min_obs}{Integer or \code{NULL}. Keep only subjects with at least this many
observations.}

\item{subject}{Optional vector naming the subject(s) to analyse.}
}
\value{
A \code{var_result} object with temporal OLS coefficients, residual
covariance, residual precision, contemporaneous partial correlations, and
tidy access through \code{\link[=edges]{edges()}}, \code{\link[=coefs]{coefs()}}, \code{\link[=nodes]{nodes()}}, and \code{\link[=summary]{summary()}}.
}
\description{
Fits a transparent VAR(1) baseline from intensive longitudinal data using
ordinary least squares: current variables are regressed on an intercept and
lag-1 predictors. The lag construction, scaling, within-person centering,
and day-boundary behavior match \code{\link[=fit_graphical_var]{fit_graphical_var()}}, but no regularization or
EBIC model selection is applied.
}
\examples{
set.seed(1)
d <- data.frame(id = 1, A = rnorm(80), B = rnorm(80), C = rnorm(80))
fit <- fit_var(d, vars = c("A", "B", "C"), id = "id")
edges(fit)
}
