% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glasso.R
\name{glasso_fit}
\alias{glasso_fit}
\title{Fit a graphical lasso at a fixed penalty}
\usage{
glasso_fit(
  S,
  rho,
  penalize_diagonal = FALSE,
  zero = NULL,
  max_outer = 10000,
  tol_outer = 1e-08,
  max_inner = 10000,
  tol_inner = 1e-10
)
}
\arguments{
\item{S}{Covariance or correlation matrix (\verb{p x p}, symmetric, finite).}

\item{rho}{Either a single non-negative penalty applied to every
off-diagonal entry, or a \verb{p x p} non-negative matrix of element-wise
penalties.}

\item{penalize_diagonal}{Logical. Penalise the diagonal of \code{Theta} as well?
Default \code{FALSE}, matching the usual EBIC-glasso convention.}

\item{zero}{Optional two-column matrix of \verb{(row, col)} index pairs whose
\code{Theta} entries are hard-constrained to zero. Used for the unpenalised
refit of a selected edge set. The diagonal is never constrained.}

\item{max_outer, tol_outer}{Outer (column-sweep) iteration cap and
convergence tolerance.}

\item{max_inner, tol_inner}{Inner (coordinate-descent) iteration cap and
convergence tolerance.}
}
\value{
An object of class \code{glasso_result}: a list with
\describe{
\item{\code{wi}}{\verb{p x p} estimated precision matrix \eqn{\Theta}.}
\item{\code{w}}{\verb{p x p} estimated (regularised) covariance \eqn{\Theta^{-1}}.}
\item{\code{beta}}{\verb{p x p} matrix of lasso coefficients from the column
sweep, reusable as a warm start.}
\item{\code{rho}}{The penalty as supplied -- a scalar or a \verb{p x p} matrix.}
\item{\code{penalize_diagonal}}{The diagonal-penalty flag used.}
\item{\code{zero}}{The hard zero constraints applied, or \code{NULL}.}
\item{\code{S}}{The covariance the fit was made to, retained so
\code{\link[=glasso_kkt]{glasso_kkt()}} can certify the result on its own. For large \code{p} in a
resampling loop this is a real retention cost; \code{\link[=glasso_path]{glasso_path()}} does
not retain it.}
}
The \code{wi} and \code{w} element names are deliberately identical to those
returned by \code{glasso::glasso()}, so existing call sites port unchanged. Use
\code{\link[=as.data.frame]{as.data.frame()}} for a tidy one-row-per-edge partial-correlation table.
}
\description{
Solves the graphical lasso problem
\deqn{\min_{\Theta \succ 0} \; -\log\det\Theta + \mathrm{tr}(S\Theta) +
      \sum_{i \neq j} \rho_{ij} |\Theta_{ij}|}
with a pure-R block-coordinate-descent solver (Friedman, Hastie and
Tibshirani, 2008). No compiled code and no external dependency is involved,
so the estimator is available wherever R is.

The objective is strictly convex, so its minimiser is unique and a
sufficiently converged fit is \emph{the} global optimum regardless of solver.
\code{\link[=glasso_kkt]{glasso_kkt()}} certifies that directly from the stationarity conditions,
without reference to any other implementation.
}
\examples{
set.seed(1)
z <- rnorm(200)
x <- cbind(A = z + rnorm(200, 0, 0.5), B = z + rnorm(200, 0, 0.5),
           C = rnorm(200), D = rnorm(200))
fit <- glasso_fit(cov(x), rho = 0.05)
fit
as.data.frame(fit)
glasso_kkt(fit)
}
\references{
Friedman, J., Hastie, T. and Tibshirani, R. (2008). Sparse inverse
covariance estimation with the graphical lasso. \emph{Biostatistics}, 9(3),
432-441. \doi{10.1093/biostatistics/kxm045}
}
\seealso{
\code{\link[=glasso_path]{glasso_path()}} for a whole penalty path, \code{\link[=glasso_kkt]{glasso_kkt()}} for an
optimality certificate, and \code{\link[=fit_graphical_var]{fit_graphical_var()}} for the estimator that
consumes this kernel.
}
