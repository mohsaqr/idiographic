% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_registry.R
\name{models}
\alias{models}
\title{List every model the package knows about}
\usage{
models(task = NULL, available = FALSE)
}
\arguments{
\item{task}{Optional \code{"regression"} or \code{"classification"} filter.}

\item{available}{Only models whose package is installed here.}
}
\value{
A \code{data.frame} with class \code{idiographic_models}.
}
\description{
One row per model per backend: what task it can do, which package provides
it, whether that package is installed here, and what its single tunable
parameter is.
}
\details{
Models are named, not numbered: \code{fit_ml(..., model = "cart")} finds its own
backend, and \verb{estimator =} is needed only to force a particular one when a
model exists in several (\code{ridge}, \code{lasso}, \code{elastic}, \code{svm}, \code{bayes}).
}
\examples{
models()
models(task = "classification", available = TRUE)
}
