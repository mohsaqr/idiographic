% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_describe.R
\name{describe_persons}
\alias{describe_persons}
\title{Describe each person's series}
\usage{
describe_persons(
  data,
  id,
  vars = NULL,
  time = NULL,
  subject = NULL,
  detail = c("basic", "full"),
  pac_quantile = 0.9,
  pac_cutoff = NULL,
  pac_direction = c("absolute", "increase", "decrease"),
  variable = NULL,
  sort_by = NULL,
  decreasing = FALSE,
  n = NULL
)
}
\arguments{
\item{data}{Data frame of repeated measures.}

\item{id}{Person/unit ID column.}

\item{vars}{Columns to describe (any \code{\link[=fit_lm]{fit_lm()}} selector). Defaults to every
numeric column other than \code{id} and \code{time}.}

\item{time}{Optional ordering column. Supply it: without it, "successive"
means the order the rows happen to be in.}

\item{subject}{Optional person(s) to describe. Defaults to everyone.}

\item{detail}{\code{"basic"} (the default) or \code{"full"}, which adds distribution
shape, floor/ceiling occupancy, the probability of acute change, the
person's linear trend, and the longest run of identical consecutive values.}

\item{pac_quantile}{Quantile of the pooled successive changes that defines
an "acute" one. The convention is 0.9.}

\item{pac_cutoff}{Use this change size instead of a quantile of the data.
Supply it to reproduce a published cutoff, or to make \code{pac} comparable
across datasets.}

\item{pac_direction}{Whether an acute change means any large change
(\code{"absolute"}), or specifically a large rise or fall.}

\item{variable}{Optional variable(s) to keep.}

\item{sort_by}{Optional column of the result to sort by, e.g. \code{"rmssd"}.}

\item{decreasing}{Sort order when \code{sort_by} is supplied.}

\item{n}{Optional number of rows to keep.}
}
\value{
A \code{data.frame} of one row per person per variable, with class
\code{idiostats_descriptives}.
}
\description{
One row per person per variable: how much data they contributed, where they
sit, how much they move, and how strongly each occasion carries into the
next.
}
\details{
\describe{
\item{\code{n}, \code{missing}}{Usable and missing occasions -- the compliance
question, asked per person rather than for the sample as a whole.}
\item{\code{mean}, \code{sd}}{Level and overall dispersion.}
\item{\code{rmssd}}{Root mean square successive difference: the average
occasion-to-occasion \emph{change}. This is a different quantity from \code{sd},
and the pair is more informative than either alone -- a slow drift gives
a large \code{sd} with a small \code{rmssd}, and rapid oscillation gives the
reverse.}
\item{\code{autocor}}{Lag-1 autocorrelation, the usual measure of inertia or
carry-over: how much of where a person is now is explained by where they
just were.}
\item{\code{pac}}{Probability of acute change (\code{detail = "full"}): how often
this person's occasion-to-occasion change clears a bar set by the whole
sample -- by default the 90th percentile of everyone's changes. \code{rmssd}
says how \emph{large} the changes are and is dominated by a few big swings;
\code{pac} says how \emph{often} a large one happens and is not. Jahng, Wood and
Trull (2008) recommend the pair together. Being sample-relative, \code{pac}
is not comparable across datasets unless \code{pac_cutoff} is supplied.}
\item{\code{span}, \code{gap_median}, \code{gap_max}}{Only when \code{time} is given. How long
the person was observed and how far apart their occasions were. Worth
reading before trusting any lag: \code{gap_max} far above \code{gap_median} means
"the previous occasion" is not a constant amount of time.}
}

Successive differences and the autocorrelation are computed on \strong{adjacent
occasions in time order within each person}, using only pairs where both
values are present. They are never taken across a person boundary.
}
\examples{
describe_persons(srl, "name", time = "day", vars = c("effort", "efficacy"))

# One person, or the most volatile few -- without reaching into the result.
describe_persons(srl, "name", time = "day", subject = "Aisha")
describe_persons(srl, "name", time = "day", variable = "effort",
                 sort_by = "rmssd", decreasing = TRUE, n = 5)
}
