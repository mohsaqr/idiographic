% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/var_bayes.R
\name{fit_var_bayes}
\alias{fit_var_bayes}
\title{Build a Bayesian VAR(1) network (unregularized, Mplus-targeted)}
\usage{
fit_var_bayes(
  data,
  vars,
  id = NULL,
  day = NULL,
  beep = NULL,
  lags = 1L,
  scale = TRUE,
  center_within = TRUE,
  n_iter = 4000L,
  n_burnin = NULL,
  n_chains = 2L,
  thin = 1L,
  seed = NULL,
  min_obs = NULL,
  subject = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame} or matrix.}

\item{vars}{Character vector of variable names (length >= 2).}

\item{id}{Character. Person-ID column, or \code{NULL} for a single series.}

\item{day}{Character. Day/session column, or \code{NULL}.}

\item{beep}{Character. Beep/measurement column, or \code{NULL}.}

\item{lags}{Integer lag order; only \code{1} is supported.}

\item{scale}{Logical. Global standardization of each variable. Default \code{TRUE}.}

\item{center_within}{Logical. Within-person centre when >1 id (removes
between-person variance, as in \code{\link[=fit_graphical_var]{fit_graphical_var()}}). Default \code{TRUE}.}

\item{n_iter, n_burnin, n_chains, thin}{MCMC controls. Defaults \code{4000},
\code{n_iter/2}, \code{2}, \code{1}.}

\item{seed}{Integer or \code{NULL}. Base seed (chain \code{c} uses \code{seed + c}).}

\item{min_obs}{Integer or \code{NULL}. Keep only subjects with at least this many
observations.}

\item{subject}{Optional vector naming the exact subject(s) to analyse.}

\item{verbose}{Logical. Progress messages. Default \code{FALSE}.}
}
\value{
A \code{var_bayes_result} object (a cograph group with \code{temporal} and
\code{contemporaneous} netobjects) carrying \code{beta}, \code{temporal}, \code{kappa}, \code{PCC},
\code{PDC}, posterior draws, and a tidy \code{coefs()} table (posterior median, SD,
95\% CI, one-tailed p, significance by CI excluding 0).
}
\description{
Native, pure-R Bayesian VAR(1) that reproduces Mplus's Bayesian
(DSEM/time-series) estimates without needing Mplus. It is the unregularized
Bayesian counterpart of \code{\link[=fit_graphical_var]{fit_graphical_var()}}: instead of a graphical-lasso /
EBIC sparse fit, it estimates a full VAR(1) with a flat prior on the
temporal coefficients and an inverse-Wishart prior on the residual
precision, then reports the temporal network \code{B} and the contemporaneous
partial-correlation network derived from the residual covariance. With more
than one subject the data are within-person centred and pooled (as in
\code{\link[=fit_graphical_var]{fit_graphical_var()}}).
}
\examples{
\donttest{
set.seed(1)
y <- matrix(0, 200, 2)
for (t in 2:200) y[t, ] <- c(0.4, 0.3) * y[t - 1, ] + rnorm(2)
d <- data.frame(A = y[, 1], B = y[, 2])
fit <- fit_var_bayes(d, vars = c("A", "B"), n_iter = 2000, seed = 1)
print(fit)
coefs(fit)
}
}
\seealso{
\code{\link[=fit_graphical_var]{fit_graphical_var()}} (regularized GLASSO/EBIC), \code{\link[=fit_var]{fit_var()}} (OLS),
\code{\link[=fit_mlvar_bayes]{fit_mlvar_bayes()}} (multilevel Bayesian VAR).
}
