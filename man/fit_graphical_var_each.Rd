% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphical_var_each.R
\name{fit_graphical_var_each}
\alias{fit_graphical_var_each}
\title{Fit a graphical VAR for every subject}
\usage{
fit_graphical_var_each(
  data,
  vars,
  id,
  day = NULL,
  beep = NULL,
  min_obs = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame or matrix with columns for variables, and optionally
id, day, beep columns for panel/ESM data. A prepared list containing
numeric matrices \code{data_c} (current responses) and \code{data_l} (lagged design,
with or without an intercept column) is also accepted.}

\item{vars}{Character vector of variable names. May be omitted for prepared
input when \code{data_c} has column names.}

\item{id}{Character. The subject-id column (required here).}

\item{day}{Character. Name of the day/session column. Default: NULL.}

\item{beep}{Character. Name of the beep/measurement column. Default: NULL.}

\item{min_obs}{Integer or \code{NULL}. Keep only subjects with at least this
many observations (counts taken from \code{data}). Default \code{NULL}.}

\item{...}{Further arguments passed to \code{\link[=fit_graphical_var]{fit_graphical_var()}} (e.g. \code{n_lambda},
\code{gamma}, \code{scale}).}
}
\value{
A named list of \code{gvar_result} objects (class \code{gvar_list}), one element
per subject, named by subject id.
}
\description{
Applies \code{\link[=fit_graphical_var]{fit_graphical_var()}} to each subject separately, returning one
person-specific network per individual — the idiographic "all individuals"
workflow. Subjects that cannot be fit (too few lag pairs after listwise
deletion) are dropped with a warning.
}
\examples{
set.seed(2)
d <- data.frame(id = rep(1:2, each = 35),
                A = rnorm(70), B = rnorm(70))
fits <- fit_graphical_var_each(d, vars = c("A", "B"), id = "id",
                               n_lambda = 3, scale = FALSE)
names(fits)
}
