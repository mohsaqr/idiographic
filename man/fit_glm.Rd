% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_fit_glm.R
\name{fit_glm}
\alias{fit_glm}
\title{Fit pooled, subgroup and person-specific generalized linear models}
\usage{
fit_glm(
  data,
  y,
  x,
  id,
  family = "binomial",
  time = NULL,
  scope = "both",
  subgroup = NULL,
  weights = NULL,
  test_prop = 0.2,
  min_train = 10L,
  min_test = 1L,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{y}{Outcome column name.}

\item{x}{Predictors: names, numeric positions, \code{a:b} range, formula, or data frame.}

\item{id}{Person/unit ID column.}

\item{family}{GLM family name or family object. Native support covers
\code{"gaussian"}, \code{"binomial"}, and \code{"poisson"}. \code{"negbin"} fits a negative
binomial via \code{MASS::glm.nb()}, which is what overdispersed counts need --
Poisson would report standard errors that are too small.}

\item{time}{Optional ordering column.}

\item{scope}{\code{"both"} (pooled + individual), \code{"pooled"}, \code{"individual"},
\code{"subgroup"}, or \code{"all"} (pooled + subgroup + individual).}

\item{subgroup}{Optional subgroup mapping: an \code{\link[=find_subgroups]{find_subgroups()}} result, a
grouping column in \code{data}, or a named vector of labels per person.}

\item{weights}{Optional column name in \code{data} holding case weights.}

\item{test_prop}{Proportion of each person's ordered rows held out.}

\item{min_train}{Minimum complete training rows per person.}

\item{min_test}{Minimum complete held-out rows per person.}

\item{...}{Passed to the underlying fitter.}
}
\value{
An \code{idiographic_fit}.
}
\description{
Fit pooled, subgroup and person-specific generalized linear models
}
\examples{
# `srl` carries some missing values, so na.rm is needed to derive an outcome.
srl$high <- ifelse(srl$effort > median(srl$effort, na.rm = TRUE),
                   "yes", "no")
fit <- fit_glm(srl, y = "high", x = c("efficacy", "planning"), id = "name",
               family = "binomial", time = "day")
metrics(fit, overall = TRUE)
}
