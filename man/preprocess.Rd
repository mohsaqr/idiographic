% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{preprocess}
\alias{preprocess}
\title{Preprocess and audit idiographic time-series data}
\usage{
preprocess(
  data,
  vars,
  id = NULL,
  day = NULL,
  beep = NULL,
  scale = TRUE,
  center_within = TRUE,
  detrend = "none",
  checks = c("trend", "high_ar", "unit_root", "mean_shift", "sd_shift", "zero_variance"),
  delete_missings = TRUE,
  min_obs = NULL,
  subject = NULL,
  trend_alpha = 0.05,
  ar_threshold = 0.95,
  mean_shift_threshold = 0.8,
  sd_ratio_threshold = 2,
  unit_root_t_cutoff = -2.86
)
}
\arguments{
\item{data}{A \code{data.frame} or matrix with columns for variables and optional
id/day/beep columns.}

\item{vars}{Character vector of variable names.}

\item{id}{Character. Name of the person-ID column, or \code{NULL} for a single
series.}

\item{day}{Character. Name of the day/session column, or \code{NULL}.}

\item{beep}{Character. Name of the measurement-occasion column, or \code{NULL}.}

\item{scale}{Logical. Whether to standardize variables before lagging.
Default \code{TRUE}.}

\item{center_within}{Logical. Whether to center within person when more than
one id is present. Default \code{TRUE}.}

\item{detrend}{How to remove non-stationarity from each series before
lagging. Either a single string applied to every variable, or a
\strong{named character vector} giving a per-variable method (unlisted variables
are left untouched, e.g. \code{c(planning = "difference", value = "linear")}).
The available methods are:
\describe{
\item{\code{"none"}}{Default; diagnose only, transform nothing.}
\item{\code{"auto"}}{Detrend only the subject-series that are flagged, leaving
the stationary ones untouched: differencing a stochastic trend (unit
root or near-unit-root persistence) and linearly detrending a
deterministic trend. The "clean whoever needs it" option -- no
subsetting, one call over everyone. Can be set per variable too.}
\item{\code{"linear"}}{Replace the series with the residuals of a within-person
regression on a linear time index.}
\item{\code{"difference"}}{First-difference the series within id/day blocks.}
}
The diagnostics and the returned design reflect the detrended series, so
the trend and unit-root flags can be rechecked after cleaning.}

\item{checks}{Character vector selecting which stationarity checkups to run:
any of \code{"trend"}, \code{"high_ar"}, \code{"unit_root"}, \code{"mean_shift"}, \code{"sd_shift"},
\code{"zero_variance"}. Defaults to all of them. Deselecting a check turns its
flag off in the report, in the \code{flag_stationarity_risk} roll-up, and in the
\code{"auto"} detrend decision, so you can screen for only what you care about.}

\item{delete_missings}{Logical. If \code{TRUE}, \verb{$pairs} contains only complete
current/lagged rows; if \code{FALSE}, first rows of blocks and incomplete rows
are retained with \code{NA} lags, matching \code{.gvar_tsdata()}. Default \code{TRUE}.}

\item{min_obs}{Integer or \code{NULL}. Keep only subjects with at least this many
observations.}

\item{subject}{Optional vector naming the subject(s) to preprocess.}

\item{trend_alpha}{Numeric p-value cutoff for the trend flag. Default \code{0.05}.}

\item{ar_threshold}{Numeric absolute AR(1) cutoff for the high-persistence
flag. Default \code{0.95}.}

\item{mean_shift_threshold}{Numeric absolute standardized split-half mean
shift cutoff. Default \code{0.8}.}

\item{sd_ratio_threshold}{Numeric split-half SD ratio cutoff. Default \code{2}.}

\item{unit_root_t_cutoff}{Numeric cutoff for the ADF-style lag-level
t-statistic. Values greater than this cutoff are flagged as unit-root risk.
Default \code{-2.86}, a common large-sample intercept-only screening cutoff.}
}
\value{
A \code{preprocess_result} object with:
\describe{
\item{\code{pairs}}{The ordered current/lagged design table, including
\code{intercept} and \verb{L1_*} columns.}
\item{\code{counts}}{Per-subject/per-day row and lag-pair counts.}
\item{\code{diagnostics}}{Per-subject/per-variable missingness, trend, AR(1),
split-half drift, unit-root screen, and stationarity risk indicators.}
\item{\code{matrices}}{The exact \code{data_c} and \code{data_l} matrices returned by
the VAR/GVAR preprocessing path.}
}
}
\description{
Builds the same lag-1 design used by \code{\link[=fit_graphical_var]{fit_graphical_var()}} and \code{\link[=fit_var]{fit_var()}},
optionally detrends or differences each series, and returns tidy diagnostics
for missingness, day-boundary drops, simple linear trends, AR(1) persistence,
split-half mean/variance drift, an ADF-style unit-root screen, and
zero-variance variables. It makes the modelling input explicit before
estimating VAR, graphical VAR, uSEM, GIMME, or mlVAR models; with \code{detrend}
it also cleans a non-stationary series in place so the flags can be
rechecked on the transformed data.
}
\examples{
set.seed(1)
d <- data.frame(id = 1, day = 1, beep = 1:40,
                A = cumsum(rnorm(40)), B = rnorm(40))
pp <- preprocess(d, vars = c("A", "B"), id = "id", day = "day", beep = "beep")
pp$counts
pp$diagnostics
# Difference the trending series and recheck the flags:
preprocess(d, vars = c("A", "B"), id = "id", day = "day", beep = "beep",
           detrend = "difference")$diagnostics
}
