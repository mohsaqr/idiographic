% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_rolling.R
\name{fit_rolling}
\alias{fit_rolling}
\title{Rolling-origin validation for ordered repeated measures}
\usage{
fit_rolling(
  data,
  y,
  x,
  id,
  method = c("lm", "glm", "ml"),
  time = NULL,
  scope = "both",
  subgroup = NULL,
  initial = NULL,
  assess = 1L,
  step = NULL,
  folds = 5L,
  min_train = 10L,
  tune = FALSE,
  valid_prop = 0.2,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{y}{Outcome column name.}

\item{x}{Predictors: names, numeric positions, \code{a:b} range, formula, or data frame.}

\item{id}{Person/unit ID column.}

\item{method}{Which family to fit each fold with: \code{"lm"}, \code{"glm"}, or
\code{"ml"}.}

\item{time}{Optional ordering column.}

\item{scope}{\code{"both"} (pooled + individual), \code{"pooled"}, \code{"individual"},
\code{"subgroup"}, or \code{"all"} (pooled + subgroup + individual).}

\item{subgroup}{Optional subgroup mapping: an \code{\link[=find_subgroups]{find_subgroups()}} result, a
grouping column in \code{data}, or a named vector of labels per person.}

\item{initial}{Rows each person trains on in the first fold. Defaults to
whatever leaves room for \code{folds} folds.}

\item{assess}{Rows predicted per fold.}

\item{step}{How far the origin moves between folds. Defaults to \code{assess}
(contiguous, non-overlapping test blocks).}

\item{folds}{Number of folds.}

\item{min_train}{Minimum complete training rows per person.}

\item{tune}{Logical. Tune each fold on a validation block carved from that
fold's training rows, so no fold's test rows influence its own settings.}

\item{valid_prop}{Proportion of each fold's training rows used for tuning.}

\item{...}{Passed to the underlying fitter, e.g. \code{model} or \code{family}.}
}
\value{
An \code{idiostats_fit} whose \code{predictions} carry a \code{fold} column.
}
\description{
Instead of one hold-out block per person, the origin walks forward: train on
each person's first \code{initial} rows and predict the next \code{assess}, then move
the origin on by \code{step} and repeat. Every fold trains only on the past, so
the result is a forecast, not a fit.
}
\details{
Predictions gain a \code{fold} column. Metrics pool across folds, so \code{metrics()}
answers "how well does this model forecast this person over time" rather than
"how well did it do on one arbitrary split".
}
\examples{
fit <- fit_rolling(srl, y = "effort", x = "efficacy:monitoring", id = "name",
                   time = "day", method = "ml", model = "ridge", folds = 3)
metrics(fit, overall = TRUE)
}
