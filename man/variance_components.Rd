% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_within_between.R
\name{variance_components}
\alias{variance_components}
\alias{variance_components.data.frame}
\alias{variance_components.idiostats_wb}
\title{Split repeated-measures variance into within- and between-person parts}
\usage{
variance_components(x, ...)

\method{variance_components}{data.frame}(x, vars = NULL, id, method = c("anova", "reml"), ...)

\method{variance_components}{idiostats_wb}(x, ...)
}
\arguments{
\item{x}{A data frame of repeated measures, or a \code{\link[=fit_within_between]{fit_within_between()}}
result.}

\item{...}{Ignored.}

\item{vars}{Columns to decompose (any \code{\link[=fit_lm]{fit_lm()}} selector). Defaults to every
numeric column other than \code{id}.}

\item{id}{Person/unit ID column.}

\item{method}{\code{"anova"} (base R, the default) or \code{"reml"} (needs \code{lme4}).}
}
\value{
A \code{data.frame} with class \code{idiostats_variance}. For a data frame:
one row per variable, with \code{variable}, \code{n}, \code{people}, \code{var_within},
\code{var_between}, \code{var_total}, \code{icc}, \code{reliability}. For a fitted model: one
row per grouping level, with \code{level}, \code{variance}, \code{sd}, \code{icc}.
}
\description{
The first question to ask of any repeated-measures variable: is the variation
mostly \emph{between} people (some people simply score higher) or \emph{within} people
(everyone moves around a lot over time)? The answer decides whether
person-specific modelling can pay off at all.
}
\details{
Called on a \strong{data frame}, the default estimator is the one-way
random-effects ANOVA, which handles unbalanced panels through the usual \code{n0}
correction rather than assuming equal numbers of occasions per person:

\deqn{\sigma^2_{between} = (MS_{between} - MS_{within}) / n_0,
      \qquad \sigma^2_{within} = MS_{within}}

\code{method = "reml"} fits \code{v ~ 1 + (1 | id)} with \code{lme4} instead. The two agree
closely on balanced data; REML is preferable when the panel is badly
unbalanced.

\code{icc} is the share of variance that lies between people. A high \code{icc} means
people differ mostly in \emph{level}, and a pooled model that ignores the person
will be badly confounded. A low \code{icc} means most of the action is
within-person, which is what idiographic modelling is for.

\code{reliability} is a different question -- how precisely each person's \emph{mean}
is measured, given how many occasions they contributed. A variable can have a
low \code{icc} (little between-person variance) and still have high \code{reliability}
(that little variance is measured well).

Called on a \code{\link[=fit_within_between]{fit_within_between()}} result fitted with \code{estimator = "reml"} or
\code{"ml"}, it returns the \strong{model's} variance components instead: one row per
grouping level plus the residual, which is what a null multilevel model is
usually run to obtain.
}
\examples{
variance_components(srl, vars = "efficacy:organizing", id = "name")
}
