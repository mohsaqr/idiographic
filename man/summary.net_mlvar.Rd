% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlvar.R
\name{summary.net_mlvar}
\alias{summary.net_mlvar}
\title{Summary method for net_mlvar}
\usage{
\method{summary}{net_mlvar}(object, ...)
}
\arguments{
\item{object}{A \code{net_mlvar} object returned by \code{\link[=fit_mlvar]{fit_mlvar()}}.}

\item{...}{Unused; present for S3 consistency.}
}
\value{
A tidy \code{data.frame} of per-network metrics (one row per network:
\code{temporal}, \code{contemporaneous}, \code{between}). Use \code{coefs(object)} for the
fixed-effect coefficient table, \code{edges(object)} for the edge list, and
\code{nodes(object)} for node strengths.
}
\description{
Summary method for net_mlvar
}
\examples{
\dontshow{if (requireNamespace("lme4", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(1)
n_id <- 8; n_t <- 30; vars <- c("A", "B", "C")
rows <- lapply(seq_len(n_id), function(i) {
  m <- as.data.frame(matrix(rnorm(n_t * 3), ncol = 3))
  names(m) <- vars
  m$id <- i; m$day <- 1L; m$beep <- seq_len(n_t)
  m
})
d <- do.call(rbind, rows)
fit <- fit_mlvar(d, vars = vars, id = "id", day = "day", beep = "beep")
print(fit)
summary(fit)
}
\dontshow{\}) # examplesIf}
}
