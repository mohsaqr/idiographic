% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glasso.R
\name{glasso_path}
\alias{glasso_path}
\title{Fit a graphical lasso over a path of penalties}
\usage{
glasso_path(
  S,
  rho,
  penalize_diagonal = FALSE,
  max_outer = 10000,
  tol_outer = 1e-04,
  max_inner = 10000,
  tol_inner = 1e-04
)
}
\arguments{
\item{S}{Covariance or correlation matrix (\verb{p x p}, symmetric, finite).}

\item{rho}{Numeric vector of non-negative penalties, used in the order
supplied.}

\item{penalize_diagonal}{Logical. Penalise the diagonal of \code{Theta} as well?
Default \code{FALSE}, matching the usual EBIC-glasso convention.}

\item{max_outer, max_inner}{Outer (column-sweep) and inner
(coordinate-descent) iteration caps, as in \code{\link[=glasso_fit]{glasso_fit()}}.}

\item{tol_outer, tol_inner}{Convergence tolerances. The defaults are the
looser path tolerances (\code{1e-4}), matching \code{glasso::glassopath()}: path
scanning selects among well-separated models, so machine precision at every
rung buys nothing. Refit the selected penalty with \code{\link[=glasso_fit]{glasso_fit()}} when the
returned matrix itself must be at full precision.}
}
\value{
An object of class \code{glasso_path_result}: a list with \code{w} and \code{wi},
each a \verb{p x p x length(rho)} array indexed in the order \code{rho} was supplied,
plus the \code{rho} vector itself. Use \code{\link[=as.data.frame]{as.data.frame()}} for a tidy
one-row-per-edge-per-penalty table.
}
\description{
Runs \code{\link[=glasso_fit]{glasso_fit()}} at every penalty in \code{rho}, warm-starting each fit from
the previous one. This is the kernel behind bootstrap and resampling
workflows that need many refits of the same covariance.
}
\examples{
set.seed(1)
z <- rnorm(200)
x <- cbind(A = z + rnorm(200, 0, 0.5), B = z + rnorm(200, 0, 0.5),
           C = rnorm(200), D = rnorm(200))
path <- glasso_path(cov(x), rho = c(0.01, 0.05, 0.2))
path
as.data.frame(path)
}
\seealso{
\code{\link[=glasso_fit]{glasso_fit()}}
}
