% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vendored.R
\name{as_netobject.net_gimme}
\alias{as_netobject.net_gimme}
\title{Plottable netobject(s) from a GIMME fit}
\usage{
\method{as_netobject}{net_gimme}(x, style = c("pnode", "unified"), weight = c("prop", "coef"), ...)
}
\arguments{
\item{x}{A \code{net_gimme} object.}

\item{style}{Either \code{"pnode"} (default) — a \code{netobject_group} of two directed
\code{p}-node networks, \verb{$temporal} (lagged; autoregression on the diagonal) and
\verb{$contemporaneous} (same-beep), matching the shape \code{\link[=graphical_var]{graphical_var()}} returns
— or \code{"unified"}, a single directed \verb{2p}-node network with the \verb{*_lag} half
feeding the current half (the literal uSEM topology).}

\item{weight}{Either \code{"prop"} (default) — edge weight is the proportion of
subjects with the path — or \code{"coef"}, the group-average standardized
coefficient.}

\item{...}{Unused.}
}
\value{
For \code{style = "pnode"}, a \code{netobject_group} with \verb{$temporal} and
\verb{$contemporaneous}. For \code{style = "unified"}, one
\code{c("netobject", "cograph_network")} object with \verb{2p} nodes.
}
\description{
Returns the GIMME result as matrix-backed netobjects. By default these encode
the \strong{same quantity the \code{gimme} package plots} — the \emph{proportion of subjects}
that have each path (\code{path_counts / n_subjects}) — not the group-average
coefficient (which dilutes toward zero and is not what GIMME displays). For
the faithful single mixed network (dashed lag / solid contemporaneous,
group/individual colouring, autoregressive self-loops) use \code{\link[=plot_gimme]{plot_gimme()}}.
}
\seealso{
\code{\link[=plot_gimme]{plot_gimme()}} for the faithful gimme-style mixed plot.
}
