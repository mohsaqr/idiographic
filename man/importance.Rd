% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_diagnostics.R
\name{importance}
\alias{importance}
\title{Feature importance}
\usage{
importance(
  x,
  model = NULL,
  scope = NULL,
  subject = NULL,
  n = NULL,
  method = c("coefficient", "permutation"),
  repeats = 5L,
  ...
)
}
\arguments{
\item{x}{An idiographic fit.}

\item{model, scope, subject}{Optional filters.}

\item{n}{Optional number of rows.}

\item{method, repeats}{Only for \code{\link[=importance]{importance()}}. \code{"coefficient"} (the default) uses the size of each
standardized coefficient, which only describes models that have
coefficients. \code{"permutation"} shuffles each predictor in the held-out rows
and reports how much worse the model gets -- model-agnostic, so it also
explains \code{knn}, forests, kernel machines and boosted ensembles, which
otherwise return nothing at all; \code{repeats} is the number of shuffles.}

\item{...}{Ignored.}
}
\value{
A tidy data frame.
}
\description{
Returns a tidy feature-importance table. For coefficient-based native models,
importance is the absolute coefficient magnitude after the model's internal
scaling. For simple tree models, the selected split variable receives the
split contrast magnitude.
}
