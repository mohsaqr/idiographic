% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/var.R
\name{fit_var_each}
\alias{fit_var_each}
\title{Fit an ordinary least-squares VAR for every subject}
\usage{
fit_var_each(data, vars, id, day = NULL, beep = NULL, min_obs = NULL, ...)
}
\arguments{
\item{data}{A \code{data.frame} or matrix with columns for variables and optional
id/day/beep columns.}

\item{vars}{Character vector of variable names.}

\item{id}{Character. Name of the person-ID column; required.}

\item{day}{Character. Name of the day/session column, or \code{NULL}.}

\item{beep}{Character. Name of the measurement-occasion column, or \code{NULL}.}

\item{min_obs}{Integer or \code{NULL}. Keep only subjects with at least this many
observations.}

\item{...}{Further arguments passed to \code{\link[=fit_var]{fit_var()}}.}
}
\value{
A named list of \code{var_result} objects (class \code{var_list}), one element
per subject, named by subject id. Subjects that cannot be fit are dropped
with a warning.
}
\description{
Applies \code{\link[=fit_var]{fit_var()}} to each subject separately, returning one transparent
person-specific OLS VAR result per individual. This is the unregularized
companion to \code{\link[=fit_graphical_var_each]{fit_graphical_var_each()}} and is useful as an equivalence baseline
for checking lag construction, scaling, and temporal coefficient direction.
}
\examples{
set.seed(1)
d <- data.frame(
  id = rep(1:3, each = 40),
  day = rep(1, 120),
  beep = rep(seq_len(40), 3),
  A = rnorm(120), B = rnorm(120), C = rnorm(120)
)
fits <- fit_var_each(d, vars = c("A", "B", "C"), id = "id",
                       day = "day", beep = "beep")
fits[["1"]]
}
